# boardwatch — Codex instructions

**Read `CLAUDE.md` in this directory now, and follow it.** It is the single source of truth for
this repository's agent instructions; this file exists only so Codex finds it.

Nothing repo-specific is written here. Anything that disagrees with `CLAUDE.md` is wrong.
