# Handoff — execution session 2026-09-08: M3's lever (D-477)

**You are the EXECUTION session, on the enterprise subscription.** Work this AFTER
`HANDOFF-2026-09-07.md`'s T40 and T41 are merged; its §5 acceptance reading (run 4) comes
after the tick is re-loaded, see below. Reasoning for every
ticket here: **D-477** and `docs/program/REVIEW-2026-09-05.md`. Do not re-derive either. Read
`STATE.md` first; if `main` has moved past what this file assumes, the repo wins.

Conventions as before: one worktree per ticket, one gate per ticket (`make check`, plain mode,
exit read from a sentinel file, never through `head`/`tail`; a contended gate is a false
negative). Red-first tests: run each new test against the unchanged code and record that it
FAILED before counting it. Report to `docs/program/REPORT-2026-09-08.md`, ≤ 150 lines, the
09-06 sections: ticket table · where this spec was wrong against the code · tests that did not
fail red first · measurements · what was not done. No AI attribution anywhere in git.

**Tick status:** the 06:00 launchd job was UNLOADED on 2026-09-05 at Mit's request so nothing runs
under your merges. When `main` holds everything from 09-07 and this file, re-load it:
`launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.boardwatch.run.plist`, verify with
`launchctl print gui/$UID/com.boardwatch.run`, and then take run 4's acceptance reading (09-07 §5).

## 0. Order, and why

| # | ticket | touches | depends on |
|---|---|---|---|
| 1 | **T45** verdict tiering in rank | `rank/`, `cli/top_cmd.py` | nothing |
| 2 | **T44** seniority title gate into the lane split | `delivery/review_gate.py` | nothing |
| 3 | **T43** split before tailor; tailor the apply lane only | `pipeline/runner.py`, `delivery/queue.py`, `PROGRAM.md` B2 | nothing, but land before T42 |
| 4 | **T42** the judge stage on the daily path | `pipeline/runner.py`, `llm/`, `core/settings.py`, funnel, digest | T43 (the judge runs where the split now runs) |
| 5 | **T46** B8 instrument | `reports/`, `METRICS.md` | T42 |

T44 and T45 are small and LLM-free; ship them first so the run has them even if T42 slips.
**All five change what is delivered, so they restart the provisional-pass count** — land them
before M2's three frozen runs are counted, not during (ROADMAP M2/M3).

## 1. T45 — the ranker never reads the verdict

`rank/heuristic.py::score_posting` weights skill 0.50 · title 0.25 · recency 0.15 · location
0.10; `cli/top_cmd.py:561` sorts on `score.total` alone, and `:573-581` drops `ineligible`
before the cut. `eligible` and `uncertain` tie.

**Spec.** Sort key becomes `(tier, -score.total)`: tier 0 = verdict `eligible` (deterministic
OR a persisted `final_gate:` `eligible`, read the way `hidden_ineligible` already reads the gate
for `ineligible`); tier 1 = `uncertain` with `role_verdict == swe`; tier 2 = everything else
still visible. Weights unchanged. **Before writing code:** confirm in `eligibility/hashing.py`
that rank weights and this ordering are NOT inputs to `rules_hash` or `engine_version`. If they
are, stop and report — that changes the ticket's blast radius.

**Red-first.** Two open postings, both visible: A `eligible` with score 0.40, B `uncertain`/swe
with 0.90. Unchanged code ranks B first; the test asserts A first. A second test: two `eligible`
postings keep score order inside the tier (so the change is a tier, not a re-weighting).

## 2. T44 — seniority reaches the lane split, title-only

`rank/seniority_gate.py::seniority_verdict(title, scheme, target_band, tier, catalog)` returns
`in_band` / `above_band` / `uncertain` and is used by `top`/`show`/reports only.
`delivery/review_gate.py::classify` (111–223) never calls it.

**Spec.** In `classify`, after the `not_swe` gate and before the `eligible` short-circuit:
`above_band` ⇒ review, new reason `seniority_above_band`. `uncertain` and `in_band` change
nothing. Count the reason in the funnel's review-lane composition (where
`experience_unconfirmed` is counted today). Do NOT read the body; that is T42's job.

**Red-first.** Title "Senior Software Engineer", verdict `uncertain`, role `swe`, location `us`,
requirement rows present: unchanged code ⇒ apply; the test asserts review with that reason.
Control: "Software Engineer, New Grad" with the same inputs stays apply.

**Measure and report:** how many of run 4's delivered leads would T44 have moved. Read-only,
`mode=ro`, over `delivered_unapplied` + `classify` — the same population the review lane uses.

## 3. T43 — split, then tailor; the review lane renders on promotion

Today `runner.py:1914` takes `leads = list(ranked.visible)` and the tailor loop at `:1957`
renders every one (40 with the plist's `--top 40`, 4.35–64.05 s each). The lane split runs
later, at queue sync (`delivery/queue.py:410`), and only picks a folder.

**Spec.** Compute each lead's lane (`review_gate.lane`, the ONE definition) before the tailor
loop. Tailor `apply` leads only. Deliver review-lane leads with no PDF and a persisted
`pending_tailor` marker (a column or disposition — pick the one `queue._sync_locked` already
reads; document the choice). `boardwatch tailor <job_id>` renders one lead on demand and clears
the marker; the web app's existing promote action calls the same function. **Check first what
`queue._sync_locked` and `_reconcile_locked` do with a lead that has no PDF** — if either
treats "no PDF" as "failed render" or "drain", that is a spec gap; report it and fix it in the
same ticket, because a review lead must never be reconciled out for lacking a file it was
never meant to have.

**B2's wording.** `PROGRAM.md` §1 B2 reads "Leads with a compiled résumé PDF — 100%". D-477
re-reads it as **apply-lane leads**. Edit the row; cite D-477 in the row.

**Red-first.** A slate of 3 apply + 2 review: unchanged code renders 5; the test asserts exactly
3 renders and 2 `pending_tailor`. Second test: promoting one review lead renders it and clears
the marker. Third: B2's instrument (wherever the acceptance table computes it) counts 3/3, not
3/5.

## 4. T42 — the judge stage on the daily path (the lever)

**What exists.** `eligibility/gate_handshake.py::build_gate_request` (44) builds the JSON
request from the shortlist; `apply_gate_verdicts` (108) applies a verdicts file through
`final_gate.record_gate_verdict` (33), which downgrades a span-less `ineligible` to `uncertain`
and writes identity-keyed `engine_kind='llm'` rows. `rank_open_postings` already hides on a
persisted gate `ineligible`. The `eligibility-judge` skill holds the blind judging protocol.
`runner.py` references none of it. The 09-06 session proved `build_gate_request` pure over 80
leads (80 → 80, 415,302 JD chars ≈ 1.3k tokens each).

**Spec.**
1. **Settings.** A `gate` block: `enabled: bool = False` (published default OFF —
   multi-tenancy), `claude_config_dir: Path | None`, `model: str = "sonnet"`,
   `batch_size: int = 13`, `call_timeout_s: int = 300`. Register it at all four gated sites
   (`Settings` field, config loader, `doctor`, the settings test — the repo's own list) and read
   the live `config.toml` key back through `Settings`; `Settings` silently ignores unknown keys.
2. **Runner.** After T43's split and before tailoring, when `gate.enabled`: build the request
   over the WHOLE delivered slate (apply and review lanes — the review lane is where release
   happens), chunk into `batch_size`, one headless call per chunk:
   `claude -p --model <model> --tools "" --max-turns 1 --output-format json`, stdin
   `</dev/null`, env `CLAUDE_CONFIG_DIR=<claude_config_dir>`, `timeout call_timeout_s`. The
   prompt is the `eligibility-judge` skill's judging contract, JD + facts ONLY — never the
   deterministic verdict, never the lane. Parse the JSON, feed `apply_gate_verdicts`, then
   re-derive each lead's lane from the persisted verdict (a gate `ineligible` leaves the slate;
   a gate `eligible` enters the apply lane; `uncertain` is unchanged).
3. **Fail-open, per D-074, at every seam.** Non-zero exit, timeout, unparseable JSON, a batch
   with the wrong item count, a missing login ⇒ record ZERO verdicts for that batch, continue
   with the slate unchanged, and count it. A judge that is down must never make a run fatal and
   must never silently look like "judged clean".
4. **Observability.** Funnel: `gate_judged`, `gate_eligible`, `gate_ineligible`,
   `gate_uncertain`, `gate_failed_open` (batches). Digest: when `gate_failed_open > 0` a SOFT
   alert line **above `_emit_morning`** in the finalize block (every soft alert sits above it;
   below it an alert is lost).
5. **Never re-judge.** Rows are identity-keyed on `(posting_version, facts, policy, catalog)`
   and self-stale; a lead with a current gate row is skipped. Cost is therefore
   `≈ new leads × 1.3k tokens + calls × harness overhead`, bounded by `--top`, never by the
   corpus. Say so in the funnel line.

**Red-first**, all through a fake `claude` binary on `PATH` (a shell script), config enabled:
(a) fake returns `ineligible` with a raw-substring span for one planted lead ⇒ that lead is
neither tailored nor delivered, and its row is `final_gate:` `ineligible`; (b) fake exits 1 ⇒
slate unchanged, run not fatal, `gate_failed_open == 1`, digest carries the soft line; (c) fake
returns `ineligible` with NO span ⇒ persisted `uncertain`, lead still delivered (end to end,
not just the unit already in `final_gate` tests); (d) `enabled=False` ⇒ no subprocess spawned
(assert via the fake writing a sentinel file that must NOT exist); (e) a lead with an existing
current gate row is not in any request.

**Calibration BEFORE the first armed tick, read-only, into METRICS.** The store holds 95 gate
verdicts from 2026-09-03 (43/38/14). Rebuild their request items with `build_gate_request` on
a `mode=ro` engine, judge them with the fake replaced by the real headless call, once with
`--model sonnet` and once with `--model haiku`, blind. Agreement with the stored rows **on the
`ineligible` axis only** (the one that survives across judges): ≥ 90% ⇒ that model qualifies;
pick the cheaper qualifying one. Plant one synthetic item that MUST come back `ineligible`
(written from `facts`, not from a real posting); if it does not, the apparatus is wrong and
nothing else is a number. Record both agreement rates.

**Machine setup — Mit's, not yours, but verify it happened:** `CLAUDE_CONFIG_DIR=~/.claude-boardwatch claude`
logged into the ENTERPRISE account once, interactively; the plist gains an
`EnvironmentVariables` dict with that variable; `gate.enabled = true` and `claude_config_dir` in
`~/.config/boardwatch/config.toml`. If the login is missing, the first tick reports
`gate_failed_open == 3` and a soft alert — that is fail-open working, not the stage working.
Confirm the exact env-var name Claude Code honours before editing the plist.

## 5. T46 — the B8 instrument

D-477 adds **B8: apply-lane leads/day ≥ 20, blind-audited unapplyable ≤ 16% on n ≥ 100.** Add
the row to `PROGRAM.md` §1 (T43 edits the same table; coordinate). Instrument: the daily count
is `apply`-lane leads delivered per run, from the funnel (T43 makes the lane known at delivery);
the defect rate is the existing blind two-judge protocol
(`audit-the-apply-lane`, `eligibility-judge` skill) over apply-lane leads, sampled to n ≥ 100
across runs, recorded in METRICS' acceptance table beside B1–B7. No automation of the audit in
this ticket — the number is read by a person, on the same cadence as B4.

## 6. Gate 1 thresholds (owner-adopted in D-477, confirmed on the T35 reading ~09-09)

Direct-ATS sources (lever, ashby, greenhouse, workday): **≥ 85%** each on
`per_source_recall.py`. LinkedIn: **not gating** (accepted loss, D-453). Indeed: **not a
recall gate** (the lane exists; D-452 owns its admission cap). hiring.cafe: **not gating**
(availability, D-463). If the 09-09 reading puts any direct-ATS source under 85%, report it as
a fact and do not move the bar.

## 7. Close

STATE (edit the head and next action — one pass, not a rewrite; move a settled block WHOLE to
`STANDING-FACTS.md` if over the ceiling), METRICS block `Session — 2026-09-08` with the T44
measurement and the calibration table, `D-478` for anything this spec got wrong, index rows by
hand, `make reindex`, full gate, exit 0 from the sentinel. Report as above. Then rewrite
`ROADMAP.md`'s M3 block if the lever landed — that is a milestone-content change, allowed.

## 8. Traps

- `Settings` ignores unknown keys — always read the live config back through `Settings`.
- Every default-context CLI command is a WRITE path; use `mode=ro` engines for every probe.
- Backgrounded `codex exec` and any stdin-reading CLI hang without `</dev/null`; the fake
  `claude` binary in tests must not read stdin either.
- A new pipeline write path can make the suite write into real user roots; pin `data_dir`
  AND `config_dir` in every new test.
- Adding a `Settings` field has four gated registration sites; miss one and `doctor` or the
  settings test fails at the gate, not at import.
- `pkill -f "make check"` is not worktree-scoped.
