# Report — session 2026-09-06 (execution)

Written for the next planning session. Reasoning: **D-473**. Numbers: `METRICS.md`, the
`Session — 2026-09-06` block. Worked `HANDOFF-2026-09-06.md` against `close-2026-09-05`.

*The handoff budgeted ≤ 120 lines and this is 151. The overage is two sections it did not
anticipate: an independent review of the combined diff (five findings, one a mutation-proven
coverage loss) and the decisions that review left owed. Compressing those to hit a line count
would drop the highest-value content for a planning session, so the budget was overrun
deliberately rather than met by cutting.*

**`main` is UNMOVED at `84671523`.** Mit ruled at the top of the session: do not merge (so the
04:00 tick delivers with a valid stamp and pays SP3 its second warm tailor measurement), and drop
dominos only. `close-2026-09-06` carries seven commits over `main`; one `--ff-only` lands them all.

## Ticket table

| ticket | outcome | gate |
|---|---|---|
| T37 — the lost board apply | **SHIPPED**, on a mechanism the spec got wrong | exit 0, 9,464 |
| T39 — T15's guard observable | **SHIPPED** | exit 0, 9,466 (with T37) |
| T38 — the scan's critical path | **SHIPPED**, as the ready queue, not the static fix | in the close gate |
| T28 — duplicate decomposition | **CLOSED** — structurally zero, no successor | read-only |
| T34 — M1 | **RESPECIFIED** — its apparatus cannot work as written | read-only |
| T35 — gate 1 re-measure | not due until 09-09 | — |
| dominos | **UNWATCHED** (Mit's ruling), 288 → 287 | — |

## Where this spec was wrong against the code

**1. T37's mechanism, and its fallback.** D-472 diagnosed starvation past `busy_timeout`, with the
fallback "raise the coordinator apply's busy_timeout". Measured before writing any fix:
**starvation does not reproduce** — a short-transaction stream let a 200 / 2,000 / 20,000-insert
writer in after **0.003 / 0.039 / 0.160 s**. The real fault is a **WAL snapshot upgrade**:
`apply_board` opens a DEFERRED transaction and READS before it writes, so **one** commit from any
other connection in that gap fails the write with `SQLITE_BUSY_SNAPSHOT` (517) — the exact string
run 3 reported — in **0.0006 s against a 5,000 ms timeout**, never consulting the busy handler.
**The prescribed fallback is not merely worse, it is impossible at any value**, and is pinned by a
test asserting ELAPSED TIME rather than the error. `store/db.py`'s `write_connection` docstring
already described this failure mode; it was not consulted.

**2. T37's consequence decision was not free.** The spec says "neither existing guard models the
pair", which is true, but not that
`test_a_fatally_refusing_run_still_lets_the_lane_thread_land_its_rows` **asserts** the behaviour
being removed. Choosing "a refused run lands no lane rows" required rewriting a shipped guard.

**3. T38's preferred design is a REGRESSION, not the smaller option.** The spec said "ship the
smaller one that passes the acceptance". Sized on the model D-344 was fitted to, recalibrated
against run 3: today 95.0 min, **static chain-first 105.5 min — worse than doing nothing**, ready
queue 83.8. Pulling a chain forward in a FIFO pool queue parks workers on a host whose lock is
already held. Only the dynamic dispatcher escapes it, which is what `host_diverse`'s own docstring
said when it declined to build it. **This supersedes D-344's "not built (the model already prices
the loss)".**

**4. T38's acceptance presumed a static order.** The shipped design has no static order — the
schedule is a function of completions — so the null control was re-expressed as an equal-cost drain
of the real `take_ready`, both arms over one fleet.

**5. T38's sizing undercounted the chain.** Not 1,931 requests (five boards' capped detail counts):
1,947 detail events across all ten boards plus ~362 listing pages = **~2,309**, so the serial floor
is **≥ 38 min, not ≥ 32**. Everything else in the diagnosis held.

**6. T28's null control as specified was insufficient.** "The probe must find the funnel's 40
before grouping" — it does, and still returns 0. Finding the population does not show the GROUPING
can report a group. The first control tried was itself vacuous (pre- vs post-collapse: both 80, so
no sibling survives either).

**7. T34's apparatus cannot work as specified, and would have failed SILENTLY.** Verified in code
and read-only in the store. `gate request` ranks with `include_handled=False`, and all 80 delivered
leads carry a permanent `built` disposition (80/80, `expires_at` and `reopened_at` NULL), so **the
judged population and `delivered_unapplied` are disjoint by construction**. `m1_probe.py`'s null
control reproduces the gate pass's OWN totals, computed before the lane join — so it PASSES while
that join is empty: the third apparatus zero of this shape in two sessions. `cli/top_cmd.py` also
hides gate-`ineligible` before the limit, so even a non-empty intersection could hold none of the
rows M1 counts. **Respecified read-only in `STATE.md` §0-1** — `build_gate_request` is pure over
anything carrying `.posting_id`, proven at 80 items / 0 dropped on a `mode=ro` URI.

**8. Two smaller ones.** `ScanSummary` already aggregated the guarded slugs, so T39's coordinator
needed no change. The dispatch loop is in `_scan_body`, not `_run_scan_locked`.

## Tests that did NOT fail red first — stated, not hidden

- `test_a_deferred_read_then_write_loses_its_snapshot_the_moment_another_connection_commits` is a
  **mechanism pin**, green before and after. Its job is to make the `busy_timeout` fallback
  permanently non-viable by asserting elapsed time, not to catch a reintroduction.
- T38 carried five tests over from the deleted ordering file that are structural, not red-first
  (exactly-once submission, per-host relative order, no-collision fleets, empty fleet,
  host-not-provider grouping). One apparent red there was an artifact of the control shim and is
  recorded as not genuine.
- Everything else was confirmed red first with the failure quoted: T37's two (`assert (2, 'lane')
  is None`; `assert {6182924288} == {8302802240}`), T38's three, T39's two.

## What the review caught, and what was done about it

An independent read of the combined diff produced five findings: three fixed, one recorded as
owner-gated, one a scope note.

1. **A real coverage loss, proven by MUTATION and now closed.** T38's replacement test file
   asserted per-host order on what `host_queues` BUILDS, not on what `take_ready` HANDS OUT, so
   `queue.pop(0)` → `queue.pop()` left all nine green while a deep host's chain would run
   newest-rowid-first. **Fixed**: the new test fails against that mutation (1 failed / 9 passed).
2. **`_lane_fetcher`'s docstring was FALSE and the behaviour behind it is LIVE.** It claimed the
   lane and the scan can never have a request in flight together. SP2 made that false and T37 keeps
   it false deliberately: two `Fetcher` instances hold independent per-host locks, so a shared host
   can see **up to 2 req/s for the lane stage's whole duration** (~352 s in run 3), not one boundary
   request — and the lanes do reach provider hosts. **Docstring corrected; behaviour NOT changed** —
   both fixes are owner questions and it is a pacing promise to third parties (`STATE.md` §0-A).
   **Not introduced this session: SP2 is already on `main` and has run twice.**
3. **The early-return enumeration named two exits; there are four** — a raised `run_scan` after
   `on_run_started` fired (Ctrl-C mid-scan) and the outer `BaseException` handler also land no lane
   rows. **Comment fixed**; only the outage case has a test.
4. **Eight comments still routed a reader to `_run_lanes`** for a path the overlapped run no longer
   takes. **Fixed.**
5. **T39's slugs reached the funnel JSON but not the MARKDOWN**, the surface the run log links to.
   **Fixed**, though within the ticket's stated scope.

Confirmed clean: the single-writer invariant (`apply_board` reached from exactly one thread after
both T37 and T38), T38's dispatch loop (no deadlock, spin, drop, double-submit or
over-subscription), and that nothing depended on T37's old error ordering.

## Decisions owed to the planning session

1. **The lane-pacing question (§2 above)** — owner-gated, and the two fixes trade against SP2's
   entire prize. Size them; do not assume the overlap survives.
2. **T40** — `apply_board` on `write_connection`, the cross-process half of T37. Proposed, not built.
3. **T34's respecified read-only procedure** — approve it, and rule on the gate's cadence.
4. **T36** — re-decide `scan_workers` on the first tick after T38, on that tick's own tail.
5. **The rest of the smartrecruiters fleet** — dominos is dropped; three remain.

## Measurements

All in `METRICS.md`, `Session — 2026-09-06`: T37's four-arm reproduction; T38's design sizing and
its two-arm null control (chain 3, 134, 147, 153… → **3, 11, 19, 27…**, 0 of 131 hosts reordered);
T28's five-scope table with the **7,093-group** positive control and run 3's shortlist drops; T34's
read-only feasibility (0 `llm` rows, 80/80 permanent `built`, 80 items buildable read-only); the
fleet before/after.

## What was NOT done, and why

- **T34 (M1).** Not run, and **respecified rather than merely carried** — see item 7 above and
  `STATE.md` §0-1. Running it as written after the tick would have produced a confident zero with
  a passing null control.
- **T40 is proposed and NOT built.** T37 removed the in-process second writer; it did not close the
  cross-process half. Every default-context CLI command here is a write path, so `top`, `doctor` or
  a `digest` run during a scan can kill a board identically. The one-line hardening is `apply_board`
  taking `write_connection` (`BEGIN IMMEDIATE`), so a contended apply queues instead of failing.
  Deliberately not shipped inside a bug fix: it changes the scan's locking discipline for every
  board and holds the write lock for a whole board apply. **Mit's call.**
- **T36** stays closed; re-decide on the first tick after T38, on that tick's own tail. `config.toml`'s
  comment still claims `le=8` against `le=32` — correct it in the next change touching Mit's config.
- **The merge**, by Mit's ruling. §0 of `STATE.md` carries the sequence unchanged.

## Acceptance owed on the next tick

T38: the smartrecruiters-alone tail (scan end minus the last non-smartrecruiters `finished_at`)
should drop from 27.6 min toward ~0, and scan wall toward `max(workday span ≈ 79 min, chain ≈ 38
min)`. T37: `boards_failed` 0. T39: read `scan.empty_complete_guarded` out of the funnel — the
readout R1 owed and could not do. All three numbers into `METRICS.md`.
