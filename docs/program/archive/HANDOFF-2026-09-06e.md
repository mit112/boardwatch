# HANDOFF 2026-09-06e — the Pinloop replacement and judge depth, executed on the enterprise seat

**Written by the planning session (Fable, personal seat) on 2026-09-06 18:20 CDT, after D-490.**
Owner's rulings this evening: *"item 3 - yeah i dont mind it. we can build that"*, then, on the
measurement that reversed item 3, *"for item 3, your recommendation"* (judge depth 150, not a
pre-screen); *"Never wanted to use Pinloop as another lane … are we missing something important?
Could we make something to replace it?"* → *"yeah lets start the replacement. If anything is token
intensive, use the enterprise subscription for execution."*

## The measured basis (all read-only, `?mode=ro`; raw in the session scratchpad)

1. **Tier-1 leads reaching the judge convert at 85%** (runs 8–9: 45 of 53 `eligible`). The judge
   sees only the delivered shortlist (40). So the lever on the 1,644-deep tier 1 is judge DEPTH,
   not a Pinloop-style metadata pre-screen (which could gain at most 15 points). → **T63.**
2. **Pinloop's employer breadth is not Workday tenants we missed.** Following the 110 "custom"
   career hosts Pinloop covers and we cannot list: **iCIMS Jibe** (AMD, JHU APL, State Farm,
   Pennymac), **Eightfold** (Northrop, Qualcomm, Applied Materials, Microsoft, Boston Scientific),
   **Phenom** (BAE, P&G, Battelle, IBM's site), **SuccessFactors** (Capgemini, EY, Cargill, XPO,
   PACCAR, Kiewit), **Oracle HCM** (Oracle 46, American Express 89, Goldman 9, Nokia, Cummins,
   Providence, Caesars), plus bespoke sites (Amazon 67, Apple 58, Meta 35, Google 15, Jane Street
   14). Direct Workday: 21 employers / 205 postings, addable with the provider we have.
3. **Endpoint viability (verified read-only 2026-09-06):** Jibe `GET /api/jobs?page=` answers with
   bodies inline and `totalCount`; Oracle HCM's `recruitingCEJobRequisitions` list answers
   (`TotalJobsCount`), its detail finder still to pin; Phenom `POST /widgets` answers with
   `totalHits` and teasers; Eightfold's `/api/apply/v2/jobs` refuses from outside the page
   ("Not authorized for PCSX").

## The tickets (one headless Opus executor each, own worktree `bw-t6N`, branch `t6N`, from `main`
at `05d8d143`; ticket text in `.agent/2026-09-06-exec/T6N.md`, rules in `COMMON.md`)

| ticket | what | key acceptance |
|---|---|---|
| **T63** | `gate.depth` (0 = today); rank+liveness+judge `depth` leads, deliver `top_n`; beyond-slate leads get NO disposition; funnel reconciles | pipeline test: depth 5 / top 2 → judge sees 5, tailor 2, dispositions for 2 only, `gate_beyond_slate == 3`, `reconciles`; second run judges none again and promotes a gate-`eligible` lead into the delivered 2 |
| **T64** | provider `jibe` (iCIMS Career Sites): slug = host, paginated single endpoint, bodies inline | contract tests incl. 2-page pagination and `board_enumerated` before a parse failure; synthetic fixtures; `FIXTURE_PROVENANCE`; registry accommodation for custom domains |
| **T65** | provider `oraclehcm`: composite `{host}/{site}`, suffix `.oraclecloud.com`, multi-endpoint like SmartRecruiters/Workday; detail finder probed first, list-only fallback declared | detail-skip and `listed_ids` tests red first; paste-URL parsing; dereference positional row |
| **T66** | provider `phenom`: composite `{host}/{country}/{lang}`, `POST /widgets` via the existing `Fetcher.post_json`, detail via widget or JSON-LD, teaser fallback marked | detail-skip and teaser-fallback red first; respx POST mocks |
| **T67** | Eightfold PROBE, 30-minute box: find an unauthenticated listing or stop with the probe log | builds only if a call exists; otherwise RESULT.md and no commit |

**Executors may not** run `make check`, touch `docs/program/`, `CHANGELOG.md`, the live store,
config, launchd, another worktree, `git stash`, or push; they must not `ruff format` a pre-existing
file. They write `.agent/RESULT.md`.

## What the planning session does when each lands
Read RESULT.md as the report, then the DIFF as the thing: (a) every "red first" claim re-run by
reverting the change locally; (b) at least one mutation per ticket of my own (T63: drop the `seen`
exclusion and expect the disposition test to fail; providers: break `board_enumerated` counting
before parse failures); (c) `make check` sequentially per branch, never two at once; (d) for T64–T66
one live `companies add --verify` against a real board in a SCRATCH data dir (`--data-dir`), never
the live store; (e) merge order T63 → T64 → T65 → T66 (→ T67), each rebased on the one before.

## Deliberately NOT in this handoff
SuccessFactors (no public JSON found; HTML-only), the bespoke majors (Amazon, Apple, Meta, Google,
Jane Street — five adapters, one employer each; next batch if the four ATS providers land), and
Workday tenant additions (a config action on the live store, done by hand after the merge).

## Ready-to-append DECISIONS text
The closing session records **D-491** with: the 85% measurement and the pre-screen refusal; the
ATS decomposition above with numbers; which of T63–T67 landed with sha, gate exit, and the
executor cost; and the per-provider live verification (board, postings fetched, `boards_failed`).
