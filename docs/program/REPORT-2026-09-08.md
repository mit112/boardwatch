# Report — execution session 2026-09-08 (enterprise sub) → for the PLANNING session

Both handoffs executed. **All six tickets are on `main`**, each with its own `make check` at exit 0
and a final integration gate over all six: **9,497 passed, 1 skipped, 4 xfailed, 7:00, exit 0**,
read from a sentinel. Reasoning: **D-478**. Numbers: METRICS `Session — 2026-09-08`.

## Ticket table

| Ticket | Commit | Gate | What moved |
|---|---|---|---|
| T40 | `b0f8d502` | exit 0, 9,470 | `apply_board` takes the write lock at BEGIN |
| T41 | `70ad0601` | exit 0, 9,471 | per-host pacing shared by every `Fetcher` in the process |
| T45 | `9ab7cbbd` | exit 0, 9,471 | rank tiers by verdict; score orders within a tier |
| T44 | `ca8ad906` | exit 0, 9,476 | `above_band ⇒ review` in `review_gate.classify` |
| T43 | `c91d6640` | exit 0 (integration) | lane computed before the tailor loop; review lane pending-tailor |
| T42 | `460ffde7` | exit 0 (integration) | headless judge stage, OFF by default, fail-open |
| T46 | — (no code) | — | B8's instrument is the funnel's `pdf` stage `entered`, made apply-lane-only by T43 |
| T34 | — (read-only) | — | executed in full, including the blind two-judge steps |
| wiring | `68923078` | exit 0 (integration) | T44's verdict computed at T43's lane site + guard test |

`main` also carries `dbe6ea3e` (web bundle rebuild) and the four merge commits.

## Where the specs were wrong against the code

**1. The 95 stored gate verdicts do not exist, and the substitute bar was vacuous.**
`WHERE engine_version LIKE 'final_gate:%'` returns **0** — they died with the 09-03 reset. The
planning session's apparatus substituted the delivered slate, but `calib_build.py` filters it to
`verdict != "ineligible"`, so its truth column is 77 `uncertain` + 3 `eligible` and **zero
`ineligible`**. An "agreement on the ineligible axis" bar over a truth column with no positives is
maximised by a judge that never says `ineligible` — it would have scored a judge that decides
nothing at 100% and ranked a decisive one below it.

Scored instead: the planted control as a **validity gate** (miss it ⇒ VOID, not low-scoring);
**sonnet vs haiku head-to-head** on the ineligible axis (needs no stored key, is the question
actually being asked, keeps ">=90% qualifies, pick the cheaper"); and disagreement-with-the-engine
**reported, never scored**.

**2. T44 is structurally unfirable.** Its flag defaulted `False` at every production caller, so
the rule could not fire; I compute it now at T43's `_lead_lanes`. That exposed the real gap:
`runner.py` calls `rank_open_postings` **without** `include_over_seniority`, so the ranker drops
above-band leads as `hidden_over_seniority` before any lane sees them. Live band is `entry`, not
`any` — the gate is armed and this is the real configuration. `moved = 0` never meant "no lead is
above band"; it meant "already dropped upstream."

**3. Landing T42 re-stamps every permanent disposition, gate OFF or not.** Measured: `config_hash`
`f56a0166…` → `200396b9…` purely from the new field. Correctly sized — **not** an eligibility
re-key (`config_hash` appears nowhere under `eligibility/`); it changes `run_policy_version`, the
stamp on `built`/`skipped` dispositions. `ledger show` reports drift; **nothing auto-reopens**.
Run 4's drift report will show every disposition stale. That is expected, not an incident.

**4. Smaller errors.** 09-07 §4 points at `.agent/2026-09-07-session/` scripts (`m1_probe.py`,
`m1_request.py`) that exist nowhere, and premises ~120 pinned rows when `max(runs.id)` is 3 and the
pin is 80. 09-08 §3 names `boardwatch tailor <job_id>`; it is `tailor run <posting_id>` and takes a
posting id, and its "the web app's existing promote action calls the same function" describes
wiring that does not exist. §2's claim that the review reason is counted in the funnel
"where `experience_unconfirmed` is counted today" is false — no funnel stage breaks the review lane
down by `ReviewReason` at all.

**5. The arming instructions name a config path `Settings` does not read.** The gate block must go
in **`~/Library/Application Support/boardwatch/config.toml`** (`BOARDWATCH_CONFIG_DIR` >
platformdirs), not `~/.config/boardwatch/`, which does not exist. Combined with `Settings`
silently ignoring unknown keys, arming could look done twice and still be inert. `CLAUDE_CONFIG_DIR`
**is** the right variable (confirmed in the installed binary; there is no `--config-dir` flag).

## Measurements

**Calibration — RULING: haiku.** 81 items (80-lead slate + planted control), 7 batches of 13, real
headless `claude -p`. Both models caught the control and returned all 81 labels; neither run void.

| | sonnet | haiku |
|---|---|---|
| head-to-head agreement | **92.6%** (75/81), kappa **0.847** | same pair |
| cost / per lead | $1.4244 / $0.01759 | **$0.6994 / $0.00863** |
| wall clock | 314 s | 548 s |
| persisted `ineligible` | 41 (50.6%) | 44 (54.3%) |

Haiku qualifies and is **2.04× cheaper**. Recorded against it: ~1.75× slower, and abstains more
(12 `uncertain` vs 3) — the safe direction for a fail-open gate.

**The near-miss band (the sizing behind the owner's ruling).** Of the post-acceptance
`experience_years` rejections, **25 (sonnet) / 28 (haiku)** sit on bars of ≤3 years where the
engine abstains by policy — **31% / 35% of the whole delivered slate**.

**T34, by lane.** apply n=5: both judges `ineligible` on 2 (40.0%), agreement 80.0%. review n=75:
35 (46.7%), agreement 89.3%. **n=5 is far below B8's n ≥ 100** — a direction, not a B8 datum, and
inflated by the near-miss band. Null controls ran: `app_state` holds 0 rows total;
`is_employer_body` rejects a synthetic aggregator body while accepting a plain JD.

**T44.** 80 delivered: 75 review / 5 apply, 80/80 `in_band`, `moved = 0` — see finding 2 for why.

## Tests that did NOT fail red first

1. **T43's promotion test** (`test_promoting_a_review_lead_renders_it_and_supersedes_the_pending_row`)
   — passed against unchanged code: pre-T43 review-lane leads were already tailored, so there was
   no pending state to promote from. Kept as a regression test only, not evidence.
2. **My T44 guard** (`test_an_above_band_title_never_reaches_the_lane_so_t44_cannot_fire`) — a
   characterization test. It asserts what the code does today and pins why the rule is dead. It
   fails the moment someone flips `include_over_seniority` without deciding what the lane should do.

Everything else was confirmed red against unchanged code with a behavioural assertion, not an
ImportError — T40 (`database is locked`), T41 (`0.0031 >= 0.95`), T45 (score-only ordering), T44
(6 tests), T43 (`assert 5 == 3` on the PDF stage), T42 (7 tests, fake `claude` on PATH).

## Also worth knowing

- **T43 widened two shared fixture `BODY` constants** (adding "Bachelor's degree preferred.") to
  repair ~35 tests the lane split legitimately broke — those fixtures now must *be* apply-lane
  leads. Verified against the real engine, and the full gate is green, but it is a shared-constant
  fixture edit and worth a reviewer's eye.
- **T42 found two `Settings` registration sites beyond the handoff's four**: `reports/manifest.py`'s
  `config_hash` exhaustiveness assert, and `tools/generalization/snapshots.py`'s pinned defaults.
- **The gate's identity carries no model dimension** — swapping `gate.model` does not force a
  re-judge of already-gated postings.
- I broke the gate once myself: edited `web/src` without `make web`, and the bundle-freshness test
  caught it by name. Fixed in `dbe6ea3e`.

## What was NOT done

- **T42 is not armed**, and its three preconditions are unmet (`~/.claude-boardwatch` absent, no
  `CLAUDE_CONFIG_DIR` in the plist, no gate block in the config). Owner's step.
- **The tick is still unloaded and run 4 has not happened.** `main` now holds everything and the
  primary checkout is parked on `main`, so the re-load is unblocked — commands in STATE "Next
  action 0". 09-07 §5's acceptance reading waits on it.
- **The healthchecks heartbeat was not paused** (its `/pause` returned 400 — not retried).
- **P1 below was not executed**, by the owner's instruction to leave it for planning.

## P1 — PENDING FOR PLANNING: lower the experience-bar floor (owner ruling, 2026-09-05)

Owner, verbatim: *"I dont want jobs which have more than 1 YOE. So 0, 1, 0-1, 0-2, 0-3, I qualify
for. Rest I dont want."* The criterion is the **floor**, and the engine already reads ranges by
floor (`range_years_minimum`: `0-3` → 0, `2-3` → 2), so it maps onto **reject when `need > 1`**.

Change: `near_miss_years_ceiling: 3 → 1` in a **personal** `~/Library/Application
Support/boardwatch/rules.yaml` override — never the bundled catalog, because stretch tolerance is
per-user and the bundled file is the generalized artifact (the live catalog is `source: bundled`).

Not done here because the costs are owner-level and coupled: it **re-keys the eligibility ledger
and owes a drain** (145,235 evaluations); the repo **content-pins `rules.yaml`**, so the pins move
in the same change; and a second population turns over — **91 corpus occurrences stating 13-36
months** are currently forced to `unknown` and would begin resolving `unmet`. The catalog says so
at the setting itself: *"Re-measure before moving it… the bound is CLOSED — 3 abstains, 4 rejects."*

Sizing is done, so it need not be re-derived: **31% / 35% of the delivered slate**, 25 and 28 of 80
leads on floors of 2-3 years.

Relationship to T42: independent, and **this is the cheaper locus**. If P1 lands, the judge stops
being the thing that rejects these and goes back to catching what the engine cannot. If it does
not, arming T42 buys the same rejection at LLM cost every run.
