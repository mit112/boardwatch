# Report — execution session 2026-09-04e (enterprise), for the planning session

Executed `HANDOFF-2026-09-04e.md`. **23 tickets merged to local `main`; 5 closed with no code on
measurement; SP3 not started and `T30` handed on as a ready, exit-0 branch.** Every gate was `make check` in the ticket's own worktree, read from its
own exit-code sentinel, never from a background notification. Over the 120-line budget because the
"spec was wrong" section is the one the handoff asked not to soften.

## Ticket table

**Every gate below was exit 2 with exactly the two known environmental failures.** Third failures
were none except where marked.

| ticket | commit | what moved |
|---|---|---|
| T20 | `a4f6c4de` | `rules_snapshot` stops hashing `catalog_source`; landed before the tick so its re-key rides the one already owed |
| T7 | `d8201f59` | corrupt facts/policy → typed `ProfileRowInvalid`, run fatal, no verdicts and no dispositions written |
| T6 | `66d7c81d` | disposition write moved below all four late fatals |
| T8 | `04211e1e` | `location_fit` word-bounded both ways |
| T10 | `e1dfa73c` | `\href` target escapes `& # %`; `{ } \` refused at pool time |
| T9 | `ab73046c` | approval screen prints every bullet |
| T11 | `891c030f` | `finish_run` guarded; the rest of the `finally` survives it |
| T12 | `5c29bd73` | scan pool cancels queued boards on abort |
| T13 | `a90e993e` | `Retry-After` > 60 s ⇒ host unreachable for the run |
| T22 | `81cfa093` | approval gates on a digest of the RESOLVED content |
| T27 | `25e32dce` | "internal test job" / "not a real job" markers |
| T18 | `78a43eec` | `_NOENG` widened by 4 words, not 6 — see below |
| SP1 | `49558a9d` | `scan_workers` ceiling 8 → 32, default still 4 |
| T25 | `d9544125` | multi-word equivalence images matched as phrases |
| T19 | `db0c9b91` | stats render test reads the table, not `inspect.getsource` |
| T21 | `ae64c0ee` | bundle lock on `add-evidence` / `resolve-conflict` — **1 third failure, mine** |
| T16 | `3abd92f8` | test-only fresh-schema fast path — different mechanism, see below |
| T26 | `21c38b57` | machine-shaped bodies held; chrome-only REFUSED on measurement |
| T15 | `4cb3679c` | an empty `complete` snapshot no longer closes a board — **16 third failures, mine** |
| T14 | `43c7f1c6` | Workday `workerSubType` facet probe deleted (1 third failure, fixed) |
| T7b | `e803c6a1` | closes the two `ProfileRowInvalid` gaps the review found |

Both third failures were real. **T21**: taking `bundle_lock` first broke `bundle_not_found`,
because `filelock` CREATES the lockfile's parent directory (`rebase.py` orders it the other way and
says why, D-138). **T15**: 16 tests used an empty board as a convenient way to close a posting;
each was repointed at a board that still lists a different posting, no assertion weakened.

## The 04:00 tick was NOT read — this session ran 17:57–20:0x on 09-04, before it

Pre-flight §1.2 instead: the **live profile row strict-validates** under merged `main`
(`Facts.model_validate` and `Policy.model_validate` both pass on the stored JSON). T7 was safe to
merge and was re-verified after.

**⚠ TONIGHT'S TICK WILL DELIVER NOTHING UNTIL MIT RE-APPROVES.** Verified by reading the file:
the one live stamp (`projection-approvals/sha256-3a292ad2…ce0ba.yaml`) carries no `content_digest`,
so T22 reads it as STALE and the `--project` preflight refuses. This is the ruled upgrade path,
but it lands tonight. Approval needs a controlling TTY, so it is his:
`boardwatch profile-bundle approve-projection`.

## Where the spec was WRONG against the code

1. **T16's ruled mechanism is unsafe.** `metadata.create_all` emits **0 of the schema's 20
   triggers** (migrated 28/29/**20**; create_all 27/28/**0**) — including the ten append-only
   `RAISE(ABORT)` pairs the keystone rests on. The ruling's safety argument
   ("`test_migrations_match_metadata` is what makes the shortcut safe") is false:
   `compare_metadata` does not see triggers. Shipped a DDL-template replay of the real chain —
   **92.9 ms → 2.9 ms (32×)** against create_all's 7.6×, schema identical. Mutation-checked.
   Second finding: **the migration chain is not byte-deterministic** — two `command.upgrade` runs
   in one process order `companies`/`board_scans` CONSTRAINT clauses differently.
2. **T22: `stamp.py` has no `candidate_content_digest` to reuse.** That name appears there only in
   the docstring explaining why the bundle's stamp type is deliberately NOT reused; added a new
   field. Also extended the digest to the resolved SKILLS section and put it on the approval
   screen — it is rendered, so leaving it out would be a hole exactly where `bundle_digest` used to
   hold. The résumé SHELL's header/education stays outside; `bundle_digest` never bound it either,
   so that narrows nothing, but it is the remaining gap.
3. **T18: the control set named in the handoff does not exist.** D-467 carries no probe table and
   neither does the review's finding 17. Used the live corpus: over 47,295 distinct open titles,
   `platform|infrastructure|machine learning|ml` release **166** titles from a hard veto; `data`
   releases **321** more and `ai` **187** more, both dominated by business roles ("Aladdin Data
   Risk, Controls & Governance, Director", "AI Physics Sales Lead - EMEA"). `data` also un-vetoes
   "Item Data Quality Analyst", which the repo's own suite pins as `not_swe`. **Shipped 4 of the 6
   ruled words**; one line adds the other two back if you disagree. Nothing moves to `swe` — every
   move is `not_swe → uncertain`, i.e. hidden → review lane.
4. **T21: no authoring entry point takes `bundle_lock`.** It appears only in `rebase.py` and
   `promotion.py`.
5. **T10: the premise held, for a different reason than stated.** A bare `\href` at top level
   carries `& # %` fine; the failure is that this emitter puts it inside a macro ARGUMENT, where
   hyperref's catcode trick cannot apply. Probing at top level would have "disproved" a real bug.
6. **T26's chrome-only class trips the ticket's own >1% stop condition** at **4.81%** (2,979 of
   61,927), on real JDs condemned for non-English or off-catalog headings. `count_section_markers
   == 0` is an English-heading detector, not a chrome detector. Not shipped.
7. **T24's premise does not hold**: all five funnel corpus sweeps are already index-covered.
8. **T17's premise does not hold either, and this is good news**: pinning `BOARDWATCH_CONFIG_DIR`
   broke **ZERO test files and ZERO tests** (full suite twice: 2 failed / 9,455 passed, both
   environmental). 73 files already pin it themselves and T2's fail-closed template work had
   already forced the render-reaching ones onto explicit config dirs. The hole was real and had
   been drained by other fixes before this session.

## The pre-reset store is gone, and it blocked four things

`eligibility_evaluations` holds only `('deterministic','1+bf844e01ebcb', 61927)` — **no final-gate
rows at all**. D-461's 95 gate verdicts, the worksheet, and the delivered set they were ranked
against all died with the 2026-09-03 reset.

- **M1: BLOCKED.** The null control returned 0/0/0 against 43/38/14 and caught it on the first try.
  `.agent/2026-09-04e-session/m1.md` records what CAN be said from the routing code (an inference,
  not a measurement): `classify` short-circuits `eligible` to APPLY, and routes an `uncertain` lead
  carrying `experience_unconfirmed` to `review:experience_requirement` — so the gate's marginal
  value in the apply lane is concentrated in D-461's 10 unambiguous verdicts, not the 28
  experience ones. **Do not quote that as a measurement.** Re-measurable after the tick
  re-evaluates and one gate pass runs; `m1_probe.py` answers it unchanged.
- **T28: BLOCKED.** 40 delivered leads, **0 groups of size ≥ 2**, against a 14–18% headline. No
  class table exists, so no identity change was taken — which is what "decompose FIRST" was for.
- **T27's hold count is 0** and **T26(b) has nothing to fix against** (max open body 29,583 chars,
  zero bodies parse as JSON).

## Also worth your ruling

- **The whole delivered queue currently reads 0 apply / 40 review**, every lead `verdict=None`,
  because the stored corpus is at `engine_version 1+bf844e01ebcb` while `current_identity` computes
  `1+d89b423701e5`. That is the owed re-evaluation, not a defect; confirmed identical against
  pre-T20 code.
- **T14's live-fleet saving**: 135 watched Workday boards, all scanned in run 2, each paying up to
  `_FACET_MAX_PAGES = 20` extra POSTs per matched bucket per run — so 0–2,700 requests per run. The
  stored snapshot does not persist the facets block, so the exact figure is not recoverable.
- **A review of this session's own 19 commits found two real defects, both from T7**, fixed as T7b:
  `GET /api/answers` crashed uncaught (the web dispatcher catches three types and `ProfileRowInvalid`
  is none of them — the connection was DROPPED), and `top`/`export`/`eligibility run`/`stats` still
  tracebacked instead of naming the column. The run always refused, so the keystone was never at
  risk; the refusal was unreadable.

## Tests that did NOT fail red first
Two, both deliberate controls: T22's `..._DOES_render_stales_the_approval` and T15's
"lists some postings still counts a miss". Each stops its fix overshooting and each has a red-first
pair. Everything else was seen red, several by reverting the source file and re-running.

## What the planning session should rule on next

1. **Should T18 also take `data` and `ai`?** Measured cost: **508 more titles** out of a hard veto
   and into the REVIEW lane the owner skims. One line either way.
2. **Is T26's chrome-only class worth rebuilding on a real chrome detector?** `count_section_markers
   == 0` is an English-heading detector; the class it was meant to catch (a body that is all site
   furniture) is real and is still uncaught.
3. **When are M1 and T28 re-run?** Both need the corpus re-evaluated AND, for M1, one gate pass.
   `m1_probe.py` and `t28_quad_decomposition.py` answer them unchanged; the cost is the judging pass.
4. **Is SP3 worth its risk now that SP2 has taken the same file?** SP3 is the last unstarted ticket
   and the only one that rewrites the tailor loop.
5. **Does the LLM final gate become routine at all?** M1 was supposed to decide this and could not.
   The routing-code reading in `m1.md` suggests its apply-lane value is concentrated in the 10
   unambiguous verdicts, not the 28 experience ones — but that is an inference, not a count.

## Not done, and where it stands
- **T30 — built, GATED AT EXIT 0, NOT merged.** Its gate on CPython 3.13.15 was
  **9,469 passed / 0 failed** — the two-failure baseline is solved and proven.   `.python-version` = 3.13 and both environmental tests rewritten to patch the fault in (both
  mutation-checked). Held back only because `uv sync` would change the interpreter under the first
  unattended run, and that should be a deliberate step and not a side effect of a gate.
  Commit `9361cec7` on branch `T30`.
- **SP2 — MERGED `d22db2a2`.** 2,327 pipeline tests pass, two-writer concurrency 11/11, gate exit 2. It changes the unattended run's shape and its first unattended exercise is the 2026-09-05 tick; read `stage_durations` there. Lanes on one daemon thread started from a
  new `on_run_started` callback. One design gap filled: the ruling did not say what happens to the
  thread on a run that returns before the join point, hence `daemon=True`.
- **T17 — MERGED `dd2db832`.** Zero test files and zero tests broke. Rebasing it onto `main` conflicted with T16 in `tests/conftest.py` (both add a pin to the same autouse fixture); resolved by keeping both, then re-verified on the four affected files before merge.
- **SP3 (+T23) — NOT STARTED.** It rewrites `run_pipeline`'s tailor loop and SP2 rewrites the same
  file's lanes stage; running both at once is the conflict the handoff's ordering exists to prevent.
- **T24 closed as "measured, not needed"**; `.agent/2026-09-04e-session/explain.md` has the plans.

**Ordering deviations.** T11/T12/T13 merged before T22 (T22's only stated dependency, T9, went
first). In the second half gates ran through a serial chain that ff'd each worktree onto whatever
`main` was at its turn, so some were gated against a `main` missing a later sibling; every such
pair touches disjoint files and the full gate on `main` at close is the backstop.
