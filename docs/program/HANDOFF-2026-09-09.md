# Handoff — execution session 2026-09-09: P1 (the ≤ 1-YoE floor), arming the judge, and run 4 (D-479)

**You are the EXECUTION session, on the enterprise subscription.** Written 2026-09-05 CDT by the
planning session after `REPORT-2026-09-08.md`. Reasoning: **D-479**. Read `STATE.md` first; if
`main` has moved past `0105dc9f`, the repo wins. Do not re-derive the haiku ruling (D-478 §1), the
near-miss sizing (METRICS `Session — 2026-09-08`), or anything in §1 below.

Conventions as before: one worktree per ticket, one `make check` per ticket in plain mode with the
exit code read from a UNIQUE sentinel file, never two gates at once, never through `head`/`tail`.
Red-first: run each new test against unchanged code and record that it FAILED on a behavioural
assertion. Report to `docs/program/REPORT-2026-09-09.md`, ≤ 150 lines, the 09-08 sections. No AI
attribution anywhere in git.

## 0. Three owner decisions, batched — put them to Mit BEFORE writing code

Nothing in §2–§4 runs until D1 is answered. Ask them together, in this order, with the numbers.

| # | decision | recommendation | what it costs / buys |
|---|---|---|---|
| **D1** | **How the ≤ 1-YoE floor is carried: as PER-USER POLICY DATA (T47, one ticket) or as a personal whole-file `rules.yaml` override (no code)** | **T47.** The override is a full copy of the bundled catalog that wins outright (`catalog.load_rules`: override file present ⇒ bundled never read), and NOTHING detects it drifting — every future catalog change on `main` would silently stop reaching the live run. The repo's own doctrine calls that a monitoring failure | T47: one execution session and one gate before run 4. Override: two minutes, run 4 today, and a silent freeze of Mit's live catalog at today's content |
| **D2** | **Order: P1 first, then arm T42 — both before run 4** | **Yes, in that order.** Gate verdicts are identity-keyed on `rules_hash` (`final_gate.record_gate_verdict` → `build_identity`), so a judge armed BEFORE the floor lands writes rows the floor then silently invalidates, and bills LLM cost for rejections the engine will make for free | Arming after P1: ≈ 40 items/run × $0.0086 ≈ **$0.35/run**, ≈ **4.5 min** wall (548 s / 81 items, haiku), enterprise seat. The judge then rejects only what the engine cannot: ~16 of 80 in the calibration once the 25–28 band leads are removed |
| **D3** | **Above-band titles: keep them HIDDEN by the ranker (T44 stays unreachable on the pipeline path)** | **Keep hidden.** Run 3 dropped **973** postings as `hidden_over_seniority` while **3,270** in-band postings cleared every filter and lost only on rank to the 40-lead cap. Surfacing the 973 cannot raise volume past the cap; it can only DISPLACE in-band leads with senior-titled ones, into the review lane, under a ≤ 1-YoE target | Nothing to build. The funnel's `hidden_over_seniority` count is the monitor, so the rule is not silent. If Mit wants them anyway: `include_over_seniority=True` at `runner.py`'s `rank_open_postings` call, retire the guard test, and decide the lane — a ticket, not a flag flip |

**Confirm the ruling's reach in one breath, with the four consequences on the page** (§1). His
verbatim ruling is an INSTRUCTION ("Rest I dont want"), which is the distinction A4/D-440 drew
between a fact and a policy move — so this is not a re-litigation of A4.

## 1. What the floor does in the engine — settled, do not re-measure

`resolve._resolve_experience_years`: `total >= need ⇒ met`; else `need <= ceiling and required ⇒
unknown`; else `unmet`. Live profile declares `total_years_experience = 1` and every family
`blocker`. With ceiling **1** and total 1 the band is EMPTY: `need <= 1` is always met, `need > 1`
is always unmet. (Ceiling 1 and ceiling 0 are identical today — D-440's table says so — but 1
states the ruling and survives a profile that ever declares 0.)

**What moves to `ineligible` (with a quoted span, keystone intact):**
1. total bars of 2–3 years — the 25 (sonnet) / 28 (haiku) of 80 delivered, 31–35% of the slate;
2. **scoped bars above 1 year** (`2+ years of Python`): `total < need` and no longer in band;
3. **months bars of 13–36 months** (13 months = 1.08 y > 1): 91 corpus occurrences, 20 with an
   engineering title, the rest retail/warehouse. Say this one out loud — it is "more than 1 YoE"
   by the ruling's own words, but nobody has put an 18-month bar to him.
4. **What does NOT move:** hedged/preferred bars (`preferred`, `nice to have`) — the band applies
   to `requiredness == "required"` only and a `preference` row cannot reject; `total >= need`
   (`0-3 years`, `1+ years`) stays `met`; scoped bars ≤ 1 y stay `unknown` (no per-skill
   durations); `extract_llm.py`'s advisory adjudication is untouched and capped below `ineligible`.

**Live store, read `?mode=ro` on 2026-09-05:** 83,308 open postings, all evaluated under ONE
`rules_hash` (`3946ef05…`), 145,235 evaluations total. The floor re-keys `rules_hash`, so **all
83,308 are pending at the next preflight** — that is the "drain": deterministic, automatic,
measured class run 138 (115,703 rows inside a 44-min run; D-394 sized the stage at ~8–12 min).
Do NOT pre-run `boardwatch eligibility run` to absorb it: it mints a `runs` row and run 4 becomes
run 5 in every doc that names it.

**Two claims in `REPORT-2026-09-08.md` P1 are WRONG and are corrected here:** the R7 pin in
`tools/generalization/allowlists.py` is a sha256 of the BUNDLED file, so a per-user change moves no
pin; and the corpus (`test_eligibility_corpus.py`, R14) runs on the bundled catalog with an empty
policy, so no corpus row changes. The 91 months occurrences turn over in the LIVE store, not in
any test.

## 2. T47 — `near_miss_years_ceiling` becomes per-user POLICY data

**Why not the file override:** whole-file replacement, no drift detection, and stretch tolerance
is a one-integer preference — the catalog already carries the per-user shape for exactly this
(`Policy.families`, severity per family, hashed via `materialised_policy`). **Why not the bundled
default:** D-333's band is the generalized artifact; another tenant with one year plus co-ops wants
it. Ship the mechanism generic, set the value locally.

**Spec.**
1. `eligibility/facts.py` — `Policy` gains `near_miss_years_ceilings: dict[str, int] =
   Field(default_factory=dict)` (family id → ceiling, `ge=0`). `extra="forbid"` stays.
2. `eligibility/catalog.py` — `RulesCatalog.effective_ceilings(policy) -> dict[str, int]` (every
   family: the policy's value or the family's declared one) and
   `RulesCatalog.effective_family(family_id, policy) -> FamilySpec` via `dataclasses.replace` on
   the frozen spec. Validation at the CLI boundary, not here.
3. `eligibility/hashing.py` — `rules_snapshot` gains `"near_miss_years_ceilings"` holding ONLY the
   entries that DIFFER from the catalog default, and only when non-empty. Two properties, both
   tested: an unset policy and an explicit-equal-to-default policy hash byte-identically (D-P2-2:
   never two fingerprints for one behaviour); landing T47 therefore moves NOBODY's `rules_hash`
   — the re-key happens when Mit sets the value, once.
4. `eligibility/engine.py:229` — `resolve(detection, facts, catalog.effective_family(
   detection.family, policy))`. `evaluate` already holds `policy`. Grep every other `resolve(`
   caller and route each through the same accessor; the reason strings in `resolve.py` read the
   spec's field and follow automatically.
5. `cli/eligibility_cmd.py` — `boardwatch eligibility policy ceiling <family> <years>`, beside
   `policy set`. Refuse (BadParameter, valid list in the message) a family the catalog does not
   declare a ceiling on — only `experience_years` today — and a non-integer or negative value.
   `init` is unchanged: `Policy()` still materialises the catalog defaults.

**Red-first tests (behavioural assertions, each red on unchanged code):**
- (a) resolve: policy ceiling 1, total 1 — `2+ years of experience required.` ⇒ `unmet`;
  `1+ years` ⇒ `met`; `18 months of experience` ⇒ `unmet`; `2+ years of Python` ⇒ `unmet`;
  `Preferred: 3+ years` ⇒ unchanged.
- (b) hashing: `Policy()` vs `Policy(near_miss_years_ceilings={"experience_years": 3})` — equal
  `rules_hash`; `{"experience_years": 1}` — different. And the snapshot is byte-identical to the
  hashed payload (the existing invariant test extends to the new key).
- (c) engine end to end: one posting body stating `2-3 years of experience` evaluates
  `uncertain` under `Policy()` and `ineligible` with a raw-substring span under ceiling 1.
- (d) CLI: sets and persists; refuses `work_auth` and `-1`.
- (e) corpus: unchanged — `test_eligibility_corpus.py` must not move a row (proves neutrality).

**Blast radius, enumerated.** No `Settings` change (the four-site rule does not apply). No
`rules.yaml` change: R7 pin unchanged, R14 corpus unchanged,
`test_a_family_declaring_no_ceiling_is_unchanged` unchanged. `web/src` reads no policy field
(grep confirmed) — no `make web`. `profile_bundle`'s `SurfacePolicy` is unrelated.

**The owner step, after merge, with NO run in flight:**
```
boardwatch eligibility policy ceiling experience_years 1
```
This is a WRITE to the live profile row. Verify through a different path: Python `?mode=ro` on
`profile.eligibility_policy_json` shows the key; run 4's funnel reports a `rules_hash` that is not
`3946ef05…`.

**The ledger drain is DECLINED, by the standing test, and the check is recorded here.**
`job_dispositions` on 2026-09-05: **80 rows, all `built`, all live-permanent, ZERO `skipped`**. A
drain reopens permanent dispositions; the only ones are `built`, and the floor is strictly
narrowing (`unknown → unmet` only), so a reopened row can only re-deliver a posting he has seen or
one the new identity now hides as `hidden_ineligible`. Upside zero. Do **not** run `ledger reopen
--stale`. Run 4's drift report will list all 80 as stale regardless (T42's `config_hash`, D-478
§3) — expected, not an incident.

## 3. T48 — arm the judge (owner's config; after T47's value is set)

Two of three steps are DONE (D-478): `~/.claude-boardwatch` is logged in on the enterprise seat
and answers headlessly; the plist carries `CLAUDE_CONFIG_DIR`. The third is the block, in
**`~/Library/Application Support/boardwatch/config.toml`** (what `Settings` reads;
`~/.config/boardwatch/` does not exist):
```toml
[gate]
enabled = true
model = "haiku"
claude_config_dir = "<home>/.claude-boardwatch"
```
`claude_config_dir` must be the EXPANDED absolute path: neither `Settings` nor `gate_judge` calls
`expanduser`, and the value is handed to the subprocess env verbatim, so a `~` would reach `claude`
as a literal. It is what a MANUAL run in the sitting relies on — only the launchd plist exports
`CLAUDE_CONFIG_DIR`. `model` MUST be explicit — the default is `sonnet`, 2.04x the cost. `Settings` and `GateTier`
both ignore unknown keys silently, and `boardwatch config show` does NOT print `gate.*`, so read
it back through the loader and assert, e.g.
```
.venv/bin/python -c "from boardwatch.core.settings import load_settings; g=load_settings().gate; assert g.enabled and g.model=='haiku', g; print(g)"
```
**T49 (small, optional, same session):** teach `config show` the four `gate.*` keys, the way it
prints `llm.*` — the operator currently has no CLI read-back for the one setting that spends money.

**Expect on the first armed run:** `gate_judged` ≤ 40 (bounded by `--top`; leads with a current
gate row are skipped), `gate_failed_open = 0`; a non-zero `gate_failed_open` with a soft alert is
the login or the binary, not the judge. Cost ≈ $0.35, wall ≈ 4.5 min.

## 4. Run 4 — the sequence, and what to read

1. T47 merged on `main`; primary checkout parked on `main` (the venv is editable).
2. `boardwatch eligibility policy ceiling experience_years 1` (§2), verified.
3. The `[gate]` block (§3), verified through the loader.
4. Re-load the tick — `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.boardwatch.run.plist`,
   then `launchctl print gui/$UID/com.boardwatch.run` (NOT `list`); it fires 06:00 CDT until a
   reboot — or run `boardwatch run --project --top 40` by hand in the sitting.
5. Read run 4: `HANDOFF-2026-09-07.md` §5 unchanged (T38 tail, `boards_failed`, T39's guard, the
   T36 rule), PLUS: the eligibility stage's minutes and `evaluated` (expect ≈ 83k), the new
   `rules_hash`, the `gate_*` counters, the lane split (apply/review of 40) and the review lane's
   `experience_unconfirmed` share (39 of 80 before), `ledger show --stale` = 80 (expected).
6. The provisional-pass count (3 frozen clean runs) starts at run 4. Nothing eligibility-side
   changes after step 2 until it is banked.

The healthchecks heartbeat (1 d + 2 h grace) alerts on the missed ping unless paused in its
dashboard; `/pause` on the ping URL returns 400 — that is Mit's dashboard action, not yours.

## 5. Traps specific to this work

- `resolve()` takes a `FamilySpec`, not a policy — put the effective spec in the catalog accessor,
  not a new resolver argument, or every resolver signature moves.
- A hashing change that adds the key UNCONDITIONALLY re-keys every tenant's ledger, including the
  test fixtures pinned on today's hashes. Conditional, differing-only, tested both ways.
- `Policy` is stored JSON on the profile row; `extra="forbid"` means an OLD binary reading a NEW
  row refuses it (`ProfileRowInvalid`). Merge before setting the value, never the reverse.
- Every default-context CLI command is a write path; probe the live store only via Python
  `?mode=ro&uri=true`, and never while a run is in flight.
- `generalization` scans TRACKED files: `git add` before gating. A METRICS/DECISIONS append needs
  its index row (METRICS rows reproduce the WHOLE heading) and `make reindex`.
