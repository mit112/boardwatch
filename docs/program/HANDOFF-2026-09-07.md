# Handoff 2026-09-07 — planning session (Fable) → next execution session

Written 2026-09-05 ~04:30 CDT by the planning session, after reading `REPORT-2026-09-06.md` and
D-473 against the code and the live store (read-only). Reasoning for everything ruled here:
**D-474**. Numbers: `METRICS.md`, the `Session — 2026-09-06b` block. The report HOLDS on every
claim checked; two of its sizings are corrected below (§2a, §2b) and neither changes a ticket
that shipped.

## 0. The state of the tree, exactly

- **`main` is UNTOUCHED at `84671523`.** Branch **`close-2026-09-06`** carries fourteen commits
  over it (T31, T32, the two 09-05 closes, T37, T39, T38, the 09-06 close, the review fixes,
  the T34 respecification, and this planning close). `git merge-base --is-ancestor main
  close-2026-09-06` was re-verified before this file was written: **ONE `--ff-only` merge lands
  them all.** It already contains every commit of `close-2026-09-05`, so that branch is not
  merged separately.
- Seven worktrees: `../bw-close`, `../bw-t31`, `../bw-t32`, `../bw-t37`, `../bw-t38`,
  `../bw-t39`, `../bw-int`. **`bw-int` holds `close-2026-09-06` — delete it LAST, after the
  merge.** Each worktree's `.venv` is that worktree's own.
- **Run 4 is the launchd tick of 2026-09-05 and it runs `main` UNCHANGED**: no T37, no T38, no
  T39; 287 boards because the dominos unwatch is live in the store. **It did NOT fire at 04:00
  CDT.** launchd (booted 09-03 23:43) computes `StartCalendarInterval` in the timezone it
  started with; the system zone was switched to America/Chicago at 23:48, five minutes after
  boot, so "Hour 4" is 06:00 CDT — which is exactly when the 09-04 tick fired (06:00:05). The
  planning session predicted a 06:00 CDT start and did not kickstart the job (a manual start
  would collide with that fire). **Verify the prediction first thing** (`runs` row 4
  `started_at` ≈ 11:00Z); if it held, the fix is Mit's: a reboot, or `Hour` in the plist. It is
  a machine-level fact, not a repo one. **Never merge with a run in flight** (`pgrep -fl
  "boardwatch run"` must find nothing — and that pattern SELF-MATCHES the shell that runs it,
  so read the `runs` table instead) — the tick runs the editable venv from whatever `main` is
  parked on.

## 1. THE MERGE AND THE RE-APPROVAL ARE ONE STEP, AND IT IS MIT'S SITTING — unchanged

`STATE.md` §0 carries the sequence verbatim and it has not changed since 2026-09-05b: no run in
flight → `git merge --ff-only close-2026-09-06` → Mit runs `boardwatch profile-bundle
approve-projection` on a controlling TTY immediately → the formatting session in the same
sitting → verify the gate read-only (`projection_candidate(...)`'s `content_digest` equals the
stamp's) → delete the seven worktrees, `bw-int` last → `git push origin main`. **Without Mit: do
not merge; do not move `main`.** T40 and T41 below are built in worktrees branched from
`close-2026-09-06`, so they need no merge to be built and gated.

## 2. Ruled this session (D-474) — do not relitigate

**2a. The lane-pacing exposure is REAL, but it is ~90 s per run on one host, not 352 s.** Read
from run 3's funnel: only two lanes ran in the stage (hiringcafe, linkedin — `grnh_seeds` is a
CLI command, and `lanes/jsonld.py`'s catalog hosts are none of the six scan hosts). The ONLY
shared-host traffic is hiringcafe's one GET per admitted board to `provider.board_url(slug)`:
**94 GETs** (greenhouse 40 → `boards-api.greenhouse.io`, ashby 44 → `api.ashbyhq.com`, lever 10
→ `api.lever.co`) spread over a 346 s fetch. The scan's own time on those hosts is **90.0 s
(greenhouse, 87 boards), 10.1 s (ashby, 40), 12.0 s (lever, 13)**. Two instances at
`per_host_delay_seconds` 1.0 (end-to-start) means ≤ 2 in flight and ≤ 2 req/s to one of those
hosts, and only inside the intersection of the two windows. `_lane_fetcher`'s docstring has the
mechanism right and the duration wrong; T41 corrects it.

**2b. The review's option framing was WRONG on one side: a shared `Fetcher` does NOT
re-serialise the lanes against the scan.** `Fetcher._dispatch` locks per HOST (`host_key(url)`),
and `hiringcafe.com` / `linkedin.com` are not scan hosts, so one instance would only ever
serialise the 94 shared-host GETs. What actually blocks a shared instance is the CLIENT: one
`httpx.Client` carries one default UA, and `lanes/linkedin.py` (two call sites) and
`lanes/github_lists.py` call `fetcher.get` with no headers, relying on the client-level browser
UA D-369 bought. **So the fix is shared per-host pacing STATE across instances with the two
clients kept** — T41 below. It costs SP2 nothing: only the 94 GETs can ever wait, each at most
one interval.

**2c. T40 is worth building, and the numbers say which side loses.** Run 3's `board_scans`
(apply = `finished_at − started_at`, board kind, n = 287): **p50 0.26 s, p90 5.15 s, p99 15.9 s,
max 16.4 s; 30 boards hold longer than the 5,000 ms `busy_timeout`**. Today ANY commit from
another process inside a deferred apply's read→write gap kills the board in 0.6 ms. With
`BEGIN IMMEDIATE` the apply queues on the write lock and a CLI write arriving during one of
those 30 boards waits up to 5 s and then fails LOUDLY — the interactive command loses, the
unattended scan does not. That is the right direction and it is a one-line change.

**2d. T34's read-only procedure is APPROVED as respecified (§4), and the gate's cadence is
DECIDED CONDITIONALLY**: it does not become routine yet. The apply lane is **5 of 80** delivered
(3 `eligible` + 2 `uncertain`), so M1's headline is a COUNT over ≤ 10 leads, never a rate. If M1
finds ≥ 1 gate-`ineligible` in the apply lane, the routine form is **per run, apply lane only**
(≤ 5 leads, ~25k jd chars, one judge) — not the 40-lead slate, whose review lane is already held
for a human look. If it finds 0, the gate stays a manual handshake. **`gate apply` is policy and
stays Mit's** either way.

**2e. T36 is re-decided by a RULE on the first post-merge run**, §5 — not before, not by feel.

**2f. The rest of the smartrecruiters fleet: SIZED, and it is not a wall-time question.** With
T38 the smartrecruiters chain is off the critical path: 9 boards ≈ 1,880 requests ≈ 31 min
serial, under the Workday span (≈ 79 min at 8 workers, ≈ 40 at 16). Dropping boschgroup,
cityofnewyork and abbvie shortens the chain to ≈ 12 min and the wall by **0 min at either worker
count**. What it buys: ~1,200 host-seconds per run on `api.smartrecruiters.com` and three
deferred backlogs (4,018 / 1,084 / 1,074) that stop growing. What it costs: **97 `eligible`
postings** (4 / 44 / 49) leave the ranked pool — none has produced a lead in runs 2 or 3. **Mit's
call; the numbers are here so it is asked once.**

Unchanged and closed: T28, T33 and its residual-zero successor, T36 as specified, T18 `data`/`ai`,
T24, T26. T37's mechanism, T38's design and the ~2,309-request chain are measured, not argued.

## 3. Tickets, in order

**Read run 4 → T40 → T41 (owner-gated) → T34/M1 → the post-merge acceptance (§5) → T35
(date-gated).** One worktree and one gate each, branched from `close-2026-09-06`, detached with
an exit sentinel, read from the sentinel. Stop where the session runs out; carry the rest by name.

### R2 — read run 4 (the 04:00 tick, `main` unchanged; read-only, S)
It is the same fleet (287) on the pre-T38 order, so it is the SAME-FLEET BASELINE for T38's
acceptance. Into METRICS: the stage-duration row against run 3; the smartrecruiters-alone tail
(scan end − the last non-smartrecruiters `board_scans.finished_at`; expect ~27 min still);
`boards_failed` (the T37 defect is live on `main`, so a lost board here is expected behaviour,
not news); tailor s/lead (**SP3 measurement 2 of 3**); delivered apply/review from each lead's
`details.json`. Traps: `started_at` is set INSIDE `apply_board` (fetch END); a failed apply
writes NO `board_scans` row; cost lives in `funnel-4.json` → `scan.fetch_cost`.

### T40 — `apply_board` takes the write lock at BEGIN (S)
`scan/apply.py:81` `with engine.begin() as conn:` → `with write_connection(engine) as conn,
conn.begin():` (`store/db.py:91`; `cli/digest_cmd.py:61` is the usage precedent — T1's begin hook
reads `BEGIN_MODE_OPTION` off the connection's execution options; confirm it fires inside
`conn.begin()` before trusting it). The lane apply (`runner.py:1104`) inherits.
- **Red first:** an `apply_board`-shaped read-then-write on one connection while a SECOND
  thread commits one row between the read and the write. Against `engine.begin()` the write
  fails with `SQLITE_BUSY_SNAPSHOT` in milliseconds (the existing mechanism pin,
  `tests/pipeline/test_two_writer_concurrency.py:76`, shows the apparatus). After the change
  the second thread's commit BLOCKS until the apply commits (it must run on its own thread with
  a join timeout, or the test deadlocks itself), the apply succeeds, and the other row lands
  after it. Assert BOTH: apply succeeded, and the other commit's timestamp is after the apply's.
  Run the real `apply_board` with a two-posting snapshot, not a probe table.
- **Stated consequence to write into the docstring:** a CLI write during one of the ~30 boards
  whose apply exceeds 5 s now fails with `database is locked` on the CLI side. That is chosen.
- Acceptance: red then green; the first post-merge run's `boards_failed` 0 with `top` or `web`
  used during the scan at least once (say whether it was).

### T41 — one per-host pacing clock per PROCESS, two clients (S; **owner-gated, §6**)
Only if Mit says fix rather than accept. In `core/politeness.py`, move `_host_locks`,
`_last_request_at` and their `_guard` out of the instance into a small pacing registry that every
`Fetcher` shares by default and a caller can inject (constructor argument, default the process
registry). Both clients and both UAs stay exactly as they are; `_lane_fetcher` keeps building
its own instance. Then rewrite `_lane_fetcher`'s docstring to state 2a's actual bound.
- **Red first:** two `Fetcher` instances, one fake host, `per_host_delay_seconds` 1.0: two GETs
  issued back-to-back from the two instances must START ≥ 1.0 s apart (today they overlap;
  assert elapsed). Control: two DIFFERENT hosts across the two instances must still overlap —
  that is SP2's prize and the test that pins it is
  `test_two_lanes_fetch_concurrently_rather_than_one_after_the_other`.
- **Blast radius to check before the gate:** 47 direct `Fetcher(` constructions in tests, 14 of
  them at delay 0. A shared registry bleeds a fake host's timestamp from one test into the next
  and adds up to 1 s per shared host per test; give the test suite a fresh registry per test
  (autouse fixture or constructor injection) and confirm the suite's wall time did not move.
  `test_the_lane_fetcher_is_a_second_instance_with_a_browser_user_agent` asserts the INSTANCE
  and the UA, not pacing independence — it stays green; update its docstring, which says
  "so per-host pacing is not shared with a provider host".
- Acceptance: the red test green; the first post-T41 run's hiringcafe `fetch_seconds` within
  ~100 s of run 3's 346 s (94 waits of ≤ 1 s is the whole priced cost).

### T34 — M1, read-only, as respecified (§4). Only with NO run in flight.

### T35 — Gate 1 re-measure (D-424, due ~2026-09-09)
`.agent/2026-09-02-session/per_source_recall.py`. Standing 28.8%. Only if the session reaches 09-09.

## 4. T34 — the read-only procedure, exactly

Nothing here writes to the store, invokes `gate request`, or runs `gate apply`. Scripts under
`.agent/2026-09-07-session/`. Engine as `m1_probe.py` opens it:
`sqlite+pysqlite:///file:<db>?mode=ro&uri=true`.

1. **Pin.** `m1_request.py`: `excluded = skipped_job_ids | reported_job_ids`; `every =
   delivered_unapplied(conn, skipped=excluded)`; write `m1_pins.json` = each row's
   `posting_id`, `job_id`, `delivered_run_id`, and its lane from `classify()` (exactly as
   `m1_probe.py` computes it), plus `max(runs.id)` at pin time. After run 4 this is ~120 rows
   (80 + run 4's slate); apply lane ≤ 10. **A two-arm read over a live store needs pinned ids** —
   every later step reads `m1_pins.json`, never the queue again.
2. **Build.** `versions = current_posting_versions(conn, ids)`; `facts =
   facts_of(get_profile(conn).eligibility_facts_json)`; `catalog = load_rules(config_dir)`;
   `request = build_gate_request(every, versions, facts, catalog, request_id=...)` → 
   `m1_request.json`. It is pure over `.posting_id`; the 09-06 session proved 80 → 80 items,
   0 dropped, 415,302 jd chars. Record `len(items)` against `len(pins)`; the difference is
   `is_employer_body` refusals and must be listed by posting id, not absorbed.
3. **Plant the control before judging.** Append ONE synthetic item whose `jd_text` states a
   REQUIRED hard stop in a catalog family the profile's `facts` fails (write it from `facts`,
   not from any real posting), labelled `control-must-be-ineligible`, lane `_control`. **Both
   judges must return `ineligible` for it with the evidence a raw substring**; if the table
   does not show it as `_control × ineligible`, the apparatus is wrong and nothing else in the
   run is a number. This is the control that can produce the answer you are not expecting —
   the last three apparatus zeros had none.
4. **Judge blind, twice.** The `eligibility-judge` skill, two fresh subagents (A and B), each
   given ONLY its batch of `items` and the skill's judging rules — never the lane, never the
   deterministic verdict, never the other judge. `verdicts_a.json`, `verdicts_b.json`.
5. **Table.** `m1_table.py`: for each judge, each verdict → `accept_oracle_verdict(v, jd_text,
   catalog)` AND `span_of(v.evidence, jd_text) is not None` (that second check is
   `record_gate_verdict`'s own downgrade — a normalised-only match persists as `uncertain`);
   join to the pinned lane on `label`; print lane × persisted-equivalent verdict per judge,
   inter-judge agreement, and **the count of apply-lane `ineligible`** with each lead's reason
   family and span. Second control: permute the labels in a COPY of one verdicts file and show
   the table changes (the join keys on the label). Third: alter one `ineligible` evidence
   string's whitespace and show it downgrades.
6. Output `.agent/2026-09-07-session/m1.md` and the table into METRICS. Then apply §2d's rule
   and write the cadence decision into STATE for Mit.

## 5. Acceptance on the FIRST `--project` run after the merge — three numbers into METRICS, then T36

Whoever launches it (a manual run in Mit's sitting or the next 04:00 tick), read it against run
4 (same fleet, pre-T38 order) with `.agent/2026-09-07-session/t38_tail.py`, read-only:
- **T38:** tail = scan end − max(`finished_at`) over non-smartrecruiters `board_scans` rows of
  that run. Expect 27.6 → ~0 min; scan wall toward max(Workday span ≈ 79 min, chain ≈ 31 min).
  Also read hiringcafe's `fetch_seconds` (T41's number if it shipped).
- **T37:** `boards_failed` 0 and no `database is locked` in `runs.errors_json`.
- **T39:** `scan.empty_complete_guarded` from `funnel-<id>.json` (a list of slugs; `[]` is a
  reading, `KeyError` is not).
Then **T36 by rule:** if tail ≤ 3 min AND `boards_failed` = 0, T36 reopens as ONE config change
with Mit's OK — `scan_workers = 16` and the comment above it corrected (`Field(default=4, ge=1,
le=32)`, not `le=8`); the model predicts **83.8 → 45.2 min**. If the tail is still > 10 min,
T38 did not deliver and `scan_workers` is not touched until that is understood. Between 3 and
10, record it and ask.

## 6. Mit's, and the question objects — asked once, at the top of a session with him present

1. **T41 — fix or accept?** Fix (recommended, S, priced at zero SP2 cost, §2a–2b) or accept
   "≤ 2 req/s on `boards-api.greenhouse.io` for ≤ ~90 s per run, less on ashby and lever". It
   is a promise to third parties, so it is his word, not ours.
2. **T40 — build?** Recommended, §2c; the stated consequence is a CLI write failing loudly
   during a long apply.
3. **The three smartrecruiters boards** — §2f, sized; 0 wall minutes either way.
4. Unchanged: `git push origin main` after the merge; `discover-candidates.yaml` (80 GitHub-list
   boards, D-291); the formatting session with the re-approval; the owner-gated items in STATE.

## 7. Close

STATE (rewrite head + next action; the file is at its ceiling — move the oldest settled block
WHOLE into `STANDING-FACTS.md`, never summarise), METRICS block `Session — 2026-09-07`, **D-475**,
index rows by hand, `make reindex`, full gate, **exit 0** read from the sentinel. Report to
`docs/program/REPORT-2026-09-07.md`, ≤ 150 lines, the same sections: ticket table, every place
this spec was wrong against the code, tests that did not fail red first, measurements, what was
not done. The 09-06 report overran its budget for the right reason; the budget is raised, not
the rule.

## 8. Traps, carried and new

- One gate at a time; detached; its own exit sentinel; never `head`/`tail`; never a background
  task's notification. A backgrounded Bash task is clamped at 10 min — use `nohup … &` with a
  sentinel file. Add index rows by hand, then `make reindex`; a METRICS row must reproduce the
  heading verbatim. Quoting a generalization violation in a doc reproduces it — name the class.
- `board_scans.started_at` is the fetch's END; a failed apply writes no row; fetch cost is only
  in the funnel. `eligibility_evaluations.input_id` is an inputs ROW id — go through
  `eligibility_inputs.posting_version_id`.
- **Three apparatus zeros in two sessions.** A control that reproduces one arm of a join, or a
  population that could never have produced the answer you are not expecting, validates
  nothing. §4 step 3 is the shape that works: plant a case whose answer MUST differ.
- The live store opens read-only ONLY through Python `sqlite3` / SQLAlchemy with
  `?mode=ro&uri=true`; the `sqlite3` CLI fails. `.venv` in a worktree is that worktree's.
  `python` is not on PATH: `.venv/bin/python`, `.venv/bin/boardwatch`, or `uv run`. `gh` is not
  installed.
- `MEMORY.md` sits 2 bytes under `build_index.py`'s hard refusal; new memories go under a
  sub-index. STATE is at its ceiling; the fix is moving a settled block whole, never a summary.
