# TICKETS — 2026-09-22c · the next sessions' work, IN ORDER: #1 audit → #2 T113 → #3 eligibility batch → #4 cleanups; #5 parked

**Written at the owner's request (2026-09-22): "make sure the tickets are properly documented so we
don't lose anything in the next session and we don't have to discover anything expensive once
again."** Every number below was measured on 2026-09-22 unless it says otherwise. Read §0 before
starting any of it: it lists what is already known and costly to re-derive.

The ORDER is the plan, and each step's reason is stated where it applies:
- **#1** is the only item that tells whether D-548's +246 apply-lane leads are good, and it needs no code.
- **#2** makes **#3** affordable. Without T113, every eligibility change strands the standing queue
  and costs a hand-run re-judge of ~30–40% of the enterprise seat.
- **#4** is small and can run alongside any of the others.

---

## 0. Already measured — do NOT re-derive

| fact | value | where |
|---|---|---|
| Seat cost of the final gate on `sonnet --effort medium`, 13 leads per call | **~3–4% of the enterprise seat per 78 leads** (owner's reading, 5% → 8%) | D-548, METRICS 2026-09-22c |
| Whole-session seat reading, 2026-09-22c | 5% → **50%**. Covers the 833-lead re-judge **plus** the working session and one reviewer on the same seat, so the re-judge alone is < 45% | this file |
| What one re-key (`rules_hash`/`profile_hash`/`engine_version`) costs | corpus re-judge **+20 min** on the next run; gate re-sends its whole slate **+8 min** (cache keyed on identity); **every standing lead loses its gate reading** (947 of 987) | D-547 |
| What a stranded standing queue does | releases gate holds (D-537, 101) AND demotes 0-B promotions (D-547, 237); **net −136** from the apply lane; never self-heals | D-547 |
| The standing-queue population | `delivered_unapplied(conn, skipped=skipped∪reported)`, verdict ≠ `ineligible`, status ≠ `closed` — exactly `delivery/queue.py:sync_queue`'s. **833** on 2026-09-22 | `delivery/queue.py:431` |
| Gate freshness key (what "already judged" means) | `(posting_version_id, profile_hash, rules_hash, engine_version exact, facts_key, model)` — **not** `effort` (T155) | `llm/gate_judge.py` `run_gate_stage`; `eligibility/read.py` `current_gate_verdicts` |
| The lane READ scopes gate rows on | `(profile_hash, rules_hash)` only, latest by `max(id)` — NOT model or facts | `store/delivery_queries.py` `delivered_unapplied` |
| `run_gate_stage` writes | **all** verdicts in **one** transaction at the END. Killing an unchunked run discards everything it judged | `llm/gate_judge.py` |
| Eligibility tables | append-only: `RAISE(ABORT)` triggers on UPDATE/DELETE, so there is **no backfill**. Old rows read "absent" | D-546 |
| Headless judge failure rate at `sonnet`/`medium` | 3 of 70 batches came back as malformed JSON (~4%). Fail-open holds; re-send recovers | D-548 |
| Live store reads | ONLY Python `sqlite3` with `?mode=ro`, or SQLAlchemy `sqlite:///file:…?mode=ro&uri=true`. The `sqlite3` CLI fails on this file | memory |
| Gate model/effort, live | `sonnet` / `medium` in `~/Library/Application Support/boardwatch/config.toml`. Backups: `config.toml.bak-presonnet-20260922`, `config.toml.bak-preeffort-20260922` | D-548 |

**Working material from 2026-09-22c** (local, gitignored, `boardwatch/.agent/2026-09-22c-session/`):
- `rejudge_backlog.py`: the one-shot standing-queue re-judge (`--shard k/n`, `--limit`, `--effort`, `--dry-run`).
- `twoarm_gate.py`: the dead-vs-live gate two-arm.
- `final_readout.py`: lanes under none / haiku / sonnet readings, plus the haiku↔sonnet comparison.
- `retest.py` and `probe469.py`, plus every batch log.

---

## #1 — T159 · Blind audit of the apply lane under the Sonnet judge (B8's precision half)

**Why first.** D-548 moved the standing apply lane from 283 to **529** leads (the old haiku readings
would have given 412). Nobody has measured whether those leads are applyable. B8's precision bar is
**≤ 16% unapplyable**. The last readings were 21.4% (the 09-13 control, haiku era) and 6.9% / 5.6%
post-drain (D-514). **529 is volume, not correctness.**

**The harness exists; reuse it, don't rebuild it.** It lives at
`boardwatch/.agent/2026-09-13-session/promotion-audit/` (local, gitignored). It is D-503's
three-arm audit:
- `stage.py` shuffles the arms into one pool with opaque ids, batches of 6, seed-fixed, and writes
  `KEY.jsonl`, which the judges never see.
- `judge.sh <A|B> <model>`: one headless `claude -p --tools "" --max-turns 1` call per batch under
  `CLAUDE_CONFIG_DIR=~/.claude-boardwatch`. It resumes by skipping finished batches.
- `PROMPT.md` holds the candidate facts and the judging policy. **It is personal data: keep it
  local, never commit it.** Regenerate it from the live profile if the facts have changed.
- `score.py` scores by arm. It uses the 2026-09-06 definition of **unapplyable** verbatim:
  **`ineligible` with a quoted span, or any fit `no` (role_family / seniority / location)**. An
  item judged by both judges counts only when BOTH say unapplyable. Keep this definition, or the
  comparison becomes a comparison of definitions.

**What to change from 09-13.**
1. **Judges.** 09-13 ran judge A = `sonnet`, judge B = `opus`, deliberately never the production
   model. **`sonnet` IS production now**, so it cannot be an independent arm (the F7 ruling's warning).
   - **Recommended:** two independent `opus` passes with a ≥ 40-item overlap slice, reporting
     inter-rater agreement.
   - **Alternative:** `opus` + `sonnet`, stating plainly that the sonnet arm is not independent.
   - **Owner's call before spending.**
2. **Arms** (all from the population in §0, built with `lane_decision` — never a hand-rolled filter):
   - `ctl`: the apply lane as it stands (529). The main reading, comparable to 09-13's 21.4%.
   - `new`: apply-lane leads that the haiku readings would have held. These are where sonnet
     differs: the 42 seniority `no → yes`, and leads promoted by a sonnet `eligible` where haiku
     said `uncertain` (147 verdict flips). This tests whether sonnet's extra clears are good.
   - `held` (optional, ~30): leads sonnet holds as `seniority_judged_above_band` (204). It tests
     the false-negative rate, the reason for the switch. Haiku's floor was ≥ 11.2% on entry titles
     (D-537); sonnet already clears 25 of those 33.
   - To build `new`, re-derive the haiku-arm lanes with `final_readout.py`'s `arm(old_g, old_s)`. The
     dead identity is `b5fafcb0…`/`6c6be0ff…`, with full hashes in `twoarm_gate.py`.
3. **Size and pacing.** 40–56 per arm, 2 judges, batches of 6, so roughly 40–60 opus calls. Opus
   per-call cost on the seat is **not yet measured**.
   - **Pacing rule (owner, 2026-09-22):** smoke batch → small chunk → STOP and ask the owner for the
     seat %. Then size the rest from that reading. Parallel shards are fine after the reading.
   - Pass `--effort medium` in `judge.sh`'s argv unless the owner says otherwise.

**Report.**
- Unapplyable by arm with 95% CIs, and inter-rater agreement.
- The cause of every unapplyable call (seniority / experience / location / clearance / …).
- **The first question to ask of every miss** (D-436): was it "no requirement row" (an extraction
  gap) or "a row was written and a rule cleared it" (a policy gap)?

**Done when.** The rates are recorded in METRICS with a DECISIONS entry. B8's precision half is
read on the Sonnet-era lane against ≤ 16%.

**Does not change** code, `rules_hash`, or the confirm clock.

---

## #2 — T113 (RE-SCOPED) · Automatic, bounded refresh of the standing queue's gate readings

**The original ticket** is `TICKETS-2026-09-19-ASTRA-02.md` §T113 (stale gate NEGATIVES suppressing
their own repair; `top_cmd` hides and promotes on a historical read). **2026-09-22 widened it.** A
stale reading is not only a negative that suppresses; every re-key also strands the POSITIVES that
0-B's promotion needs.
- The original sizing was 97 of 984 standing leads with no current gate row. After run 470 it was
  **947 of 987**.
- The repair was done by hand (D-548) with `rejudge_backlog.py`. That script is the manual form of
  this ticket.

**What to build (design first, then code).**
- A refresh step in the daily run, after the gate judges the depth slate:
  - Take standing-queue leads (§0's population) that have **no current gate reading** under the
    gate's freshness key.
  - Judge them through the SAME `run_gate_stage` (it accepts any objects with `posting_id`) within
    a **per-run budget**.
  - Order **promotable holds first** (`no_requirements_found`, `experience_requirement`, the two
    holds a judge `eligible` releases), then the apply lane, then the rest.
- **Budget** is a new `[gate]` setting. `0` means off, and that is the multi-tenant default.
  - It is a scheduling knob, so it belongs OUT of `config_hash`, like `depth`. It changes which
    leads carry a verdict at the lane split, so it goes INTO `_ROUTING_RELEVANT`.
  - It is a new `Settings` field, so it has **four gated registration sites**: `cli/config_cmd.py`
    `_GATE_KEYS`; `tools/generalization/snapshots.py` (both dicts, plus the field class);
    `reports/manifest.py`; `core/settings.py`. `make check` catches a miss; narrow pytest does not.
- **Funnel.** Report it in the funnel's `gate` block: `refresh_candidates`, `refresh_sent`,
  `refresh_pending_after`.
  - Pending staying high across runs is the signal that the budget cannot keep up.
  - Any soft alert must sit ABOVE `_emit_morning` (the finalize-block ordering invariant).
- **Chunk the writes.** `run_gate_stage` commits once at the end, so call it per chunk. A timeout
  or kill must not discard a whole refresh.
- **The original half still stands.** `top_cmd` hides on a historical `ineligible` and promotes on a
  historical `eligible` (the T113 text cites `top_cmd.py:540-545`, `:700-703`, `:724-728`; line
  numbers drift, so re-find them). A stale negative should become a review hold pending re-read,
  not a suppression.

**Size, from the 2026-09-22 calibration.**
- A full standing queue is ~833 leads, about 64 calls at 13 per call: ~30–40% of the seat in one go.
- A budget of ~130 per run (10 calls, ~5–7% of the seat per run) drains a full re-key in about 7 runs.
- In steady state (no re-key) the refresh is near 0.

**Interactions.**
- **T152** removes `rules_hash` from the SENIORITY reading's key, so rules-only edits stop killing
  seniority readings. Verdict rows stay identity-keyed, so T113 is still needed.
- **T155** adds `effort` to the freshness key; T113 should respect whatever key exists.
- **Fail direction.** D-380 still holds: absent reads `unclear`/no promotion, never `no`.
- **Tests to keep green:** `tests/unit/test_rank_gate_filter.py:142`, `:116`, and
  `tests/unit/test_rank_verdict_tiers.py:107,136,153`. Also the `lane_decision` AST guard, which
  enumerates the five call sites.

**Discriminating tests owed.**
1. A fixture re-key followed by a run: the refresh restores a 0-B promotion that the re-key demoted.
2. The budget is respected.
3. Order: promotable holds come first.
4. A failed batch leaves its leads pending, never dropped.
5. Budget `0` means no refresh at all. This is the multi-tenant default.

**Done when.** A re-key's standing-queue damage heals without a hand-run script, within a stated
number of runs, visible in the funnel.

---

## #3 — ONE eligibility batch: T152 + T156 + T157 + T105, landed together AFTER #2

**Why batched, and why after #2.** Each item moves `rules_hash` and/or `engine_version`.
- Every move costs what §0 lists, and restarts B8's 14-day confirm (`PROGRAM.md` §1).
- One batch pays that once. With T113 in place, the standing-queue repair is automatic.
- The owner's standing preference: a clock restart is a cost to state, not a reason to defer
  (memory: "shipped work beats milestone closure").

**Procedure for any `rules.yaml` change:** `STANDING-FACTS.md` §"Changing `rules.yaml` — the full
pin and corpus procedure (verified 2026-09-22)".
- 90 `pin="sha256:` literals live in `tools/generalization/allowlists.py`. Key every edit on the OLD
  value and assert exactly one replacement.
- Clear `__pycache__` between a `--record` and its `--check` (the same-second, same-size trap).
- `make reindex` after any index row. Verify by heading and row COUNTS, not by the diffstat.

**Each engine bump also owes a drain decision.** The drain has been refused six times on
measurement; the last is D-540. Re-measure rather than assume.

### T152 — Stop keying a body-seniority reading on `rules_hash` (DESIGN DONE, now UNBLOCKED)
- **Design:** `docs/program/DESIGN-T152-seniority-reading-identity.md`. A reading is valid for
  `(posting_version_id, years_key, judge_key)`.
- **Migration** is forward-only, because the tables are append-only.
- **Its load-bearing precondition, T153 before T152, is MET** (D-548: every standing lead now
  carries a sonnet reading).
- The design also notes that the seniority read is UNDER-keyed: it has no `facts_key` and no
  `model`, unlike `current_gate_verdicts`.

### T156 — Season-named graduation windows are unreadable (`TICKETS-2026-09-22.md` §T156)
- **Site:** `eligibility/resolve.py:614`, in the `graduation_window_required` branch.
  - `_yyyymm(detection.values["from_month"], …)` returns `None` for `Fall`/`Summer`, so the rule
    resolves `UNKNOWN`, "the stated window names a month this catalog cannot read".
  - The detector already captures the season token: posting 110519 arrives as
    `from_month='Fall', to_month='Summer'`.
- **Reach:** 457 of 260,581 open postings (0.18%). Measured in `.agent/2026-09-22b/notes.md:166`.
- **Needs a season → month table as catalog DATA, not code**, with a stated bound direction.
  Recommended: the low bound takes the season's FIRST month and the high bound its LAST month (the
  generous reading), since an over-tight bound turns a match into an `unmet` and deletes a job.
  Winter spans a year boundary, so state its month choice explicitly.
- **Controls:** an inverted window must still read "inverted", and an unknown token must still
  abstain.

### T157 — Company-tenure prose read as a years bar (`TICKETS-2026-09-22.md` §T157; D-544 "Half B")
- **Reach:** 49 of 407,150 unmet `experience_years` rows. Example: *"With over 25 years of
  experience serving the sector, our team is committed to…"* read as a 25-year requirement. It is
  job-DELETING, and larger than T149's measured benefit of 10.
- **Site:** `eligibility/rules.yaml:537` `&company_side_years`, used as `subject_suppressors` at
  `:600`, `:610` and `:620` (searched BEFORE the span).
- **The fix is an AFTER-span suppressor with a NARROWER anchor.** Aliasing `*company_side_years`
  there is **unsafe**: it contains `you\s+will\s+(?:work\s+with|receive|gain|get)`, which after a
  bar suppresses genuine requirements.
- **Known false-positive trap in D-544's sample:** *"Associates Degree … with <2 years pertinent
  experience"* matched the anchor. It must stay a requirement.

### T105 — Bounded heading context for bullet lists (DESIGN DONE; blocks T101 Half A)
- **Design:** `docs/program/DESIGN-T105-heading-context.md`. It uses an out-of-band unit-index →
  heading map, so `split_units`' output stays byte-identical.
- **Why byte-identical matters:** it protects the 1,076 goldens, the anti-reorder lock and
  `abstain_by_adjacent`'s `index + 1` arithmetic.
- **The golden corpus cannot express the shape** (0 of 1,076 rows contain a newline), so a new
  multi-line fixture surface is owed.
- **Once it lands, T101 Half A becomes implementable** (D-544: a JD carries its obligation
  structurally, under a heading).

---

## #4 — Small cleanups (independent of the order; any session)

### T127 — `queue_detail` never passes `requirement_flags` (pane and list disagree) — LIVE: 12 leads
- **Site:** `store/delivery_queries.py` `queue_detail` (~`:1603`). Its `_queue_row(...)` call passes
  the verdict, gate verdict, seniority, form question, band and revision, but **not**
  `requirement_flags`. The pane falls back to `NO_REQUIREMENT_FLAGS`.
- **Measured 2026-09-22c** (null control 0): **12 of 833** standing leads read `apply` in the detail
  pane while the list holds them as `_review:no_requirements_found`.
- **It scales with every re-key.** Before D-548's re-judge it would have been ~345.
- **Fix:** read `current_requirement_flags` in `queue_detail` under the same identity and version,
  and pass it.
- **Test:** the pane and the list must agree for a `no_requirements_found` lead. It must fail
  against the current code.
- **Origin:** `TICKETS-2026-09-20-ASTRA-03.md` row T127.

### T155 — Key gate freshness on `effort` too (`TICKETS-2026-09-22.md` §T155)
- **Harmless today**, because every standing row is `sonnet`/`medium`. It bites on the next change
  of level.
- **Needs** a forward-only place to record effort per gate row.
- **Owner question first:** should a level change re-judge the standing queue at all? The cost is
  in §0.

### T158 — A cited test does not exist (`TICKETS-2026-09-22.md` §T158)
- `tests/unit/test_review_gate.py:35` cites `test_the_lane_classifier_has_one_standing_call_site`.
  No such test exists.
- The boundary is actually held by `mypy --strict` plus the `_INERT` merge raising `TypeError`.
- Fix the citation, or write the test.

---

## #5 — PARKED, not forgotten: the remaining astra-review tickets

**None is on the goal's critical path. Pick them up after #1–#4, or when one starts to bite.**

| ticket | one line | source |
|---|---|---|
| T123 | separate inventory completeness from detail-acquisition completeness | `TICKETS-2026-09-20-ASTRA-03.md` |
| T124 | evidence-scoped job membership with invalidation and reconciliation (needs a migration) | same |
| T125 | evidence-graded cross-job application links — **measurement first**, not yet a build ticket | same |
| T133 | a whole-run lease covering `--no-scan`, rejecting a contender before any write; fix `WAL_DISCIPLINE.md:30` | `TICKETS-2026-09-20-ASTRA-04.md` |
| T135 | take the queue lock before establishing the read snapshot; one snapshot per filesystem plan | same |
| T137 | a frozen `RunContext` + separate execution provenance (release SHA, gate prompt fingerprint, armed lanes, fleet count) | same |
| T138 | (two remaining halves) reuse `reports/reconcile.py`'s per-file checks in the run guard; check required files by artifact kind | same |
| T139 | the stage extraction — FIRST correct `RUN_CONTRACT.md` to the 13 real fatal causes and `except BaseException` | same |
| T144 | hiring.cafe discards usable partial discovery and conceals lesser degradation | `TICKETS-2026-09-20-ASTRA-05.md` |

**Also parked, and the owner's to decide:**
- Switching job-apps off. It is the last step in the goal line. **Size it first:** boardwatch
  consumes job-apps' discovery as the `jobapps` lane, 2,877 resolved on run 470.
- The résumé calls.
- `ServiceNow Developer` ranking.
- The 17 never-listable boards.
- The projection spec's §12.
- The optional 0-D data repair.

---

## Status at the 2026-09-22d close, and two tickets it added

- **#1 T159: DONE (D-550).** The apply lane is 5.4% unapplyable (CI 2–15%), so B8's precision half
  is MET on the Sonnet-era lane. The owner call it leaves open: about 53% of Sonnet's seniority
  holds read applyable to Opus.
- **#2 T113: SHIPPED, off by default (D-551), and ARMED in the live config at 130/run on the
  owner's ruling (D-552).** #3 is unblocked.
- **#4 T127 and T158: SHIPPED.** T155 is still owner-gated.

### T113b — a stale gate `ineligible` hides a posting from ranking (the original T113 half)
- `top_cmd` reads gate rows by `(profile_hash, rules_hash)` only, and hides on `ineligible`.
- **Measured 2026-09-22d:** it hides **66** open postings on a negative that is not current under
  the freshness key (deterministic `eligible` 42, `uncertain` 24). All are haiku-era, on
  never-delivered postings. Probe: `.agent/2026-09-22d-session/stale_negatives.py`.
- **#3's `rules_hash` move frees all 66 on its own**, because that read is keyed on it. Only a
  model, effort or facts change recreates the backlog. Build it only if one recurs.

### T160 — `classify_location` fails open on an ISO-3 country suffix
- `Dublin, IRL`, `Iasi, ROU` and `Tianjin, Tianjin` classify `unknown`, so they pass as US
  candidates. `Bucharest, ROU` reads `non_us` only because the city is curated.
- **Reach, measured 2026-09-22d:** open postings whose every segment ends in a non-USA ISO-3 code
  split 2,490 `non_us` / **777 `unknown`** / 4 `us`. The control, every segment `,USA`, reads
  6,539 `us` and nothing else. 3 of the 777 stand in the apply lane; T159 caught them.
  Probe: `.agent/2026-09-22d-session/iso3_reach.py`.
- **Fix:** read the ISO-3 suffix as a country signal, as curated DATA (D-294's pattern), never by
  tightening the `unknown` fail-open. Check the 4 that read `us` first: a suffix table must not
  flip a real US posting.

---

## Status at the 2026-09-22e close, and three tickets it added

- **#3, the eligibility batch: SHIPPED as ONE engine bump (D-555).** It covers T105, T156, T157,
  T152, and **T155, folded in** on the owner's YES: the re-key already strands every reading, so one
  re-judge serves both.
  - 2,771 open verdicts move.
  - T152's legacy fallback keeps all 833 standing seniority readings.
  - The drain is refused a seventh time.
  - **T105 went beyond its design in scope:**
    - `Preferred/Desired Qualifications:` hedge;
    - a field label is read through;
    - an OR item after an inline bullet counts, which is how posting 261677 actually writes its
      ladder;
    - the reach ends where the list ends.
- **T160: SHIPPED (D-553)** on a 30-code observed inclusion list.
- **D-529: RULED and SHIPPED (D-554).**
- **Owner checkpoint (2026-09-22e):** the rest is the next session's. The order is STATE's NEXT
  WORK: run 471 → pull → Wave A (T144, T135, T124, T123) → T125 → Wave B (T133 → T137 → T138) →
  T139.
- **Review budget (owner, 2026-09-22e):** 1 review + 1 verification round per change; a third only
  for a genuine, reachable blocker. The model is `gpt-6-sol`, effort ≤ `high` (global CLAUDE.md
  "Codex reviews").

### T161 — key the LANE's gate-verdict read on the judge's inputs, not the row identity
- `delivered_unapplied`'s gate-verdict read scopes on `(profile_hash, rules_hash)`, so every
  rules-only re-key darkens every standing 0-B promotion until the T113 refresh re-judges it. Yet
  the judge is sent the JD and the facts, not the catalog (D-461's all-blocker policy).
- **Measured 2026-09-22e, for the eligibility batch's re-key:** the standing apply lane goes 529 →
  206 (−323). That's 329 promotable leads: 205 → `no_requirements_found`, 124 →
  `experience_requirement`. The refresh restores them at 130/run, promotable first, so about
  3 runs.
- **Shape:** T152's key, applied to the verdict read. That is `(posting_version_id, facts_key,
  model, exact gate engine_version)`.
- **Check first:** whether `accept_oracle_verdict(verdict, jd_text, catalog)` makes a persisted
  verdict catalog-dependent. It downgrades a span-less `ineligible` using the catalog.
- Moves D-537/D-547 territory, so it needs a design note and the owner's read before it's built.

### T162 — freshness and the lane can read different rows after a judge switch-back
- `_current_gate_rows` narrows on model/facts/effort INSIDE `max(id)`. This is T108's pinned
  design: `test_an_older_row_from_the_configured_model_still_hits_under_a_newer_one`.
- The lane reads, which don't narrow, take the NEWEST row.
- **The failure (Codex, batch review round 4):** rows `medium/eligible` then `high/ineligible`;
  switch back to `medium`. Freshness finds the older medium row and never re-judges, while the
  lane consumes the newer `high/ineligible` forever. The same holds for `model` (sonnet → haiku
  → sonnet).
- **Candidate fix:** freshness requires the lane-visible newest row to satisfy the key. That
  re-litigates T108's cache-cost argument, so it's a decision, not a bug fix.
- **Red first:** the switch-away/switch-back test Codex describes.

### T163 — a hedged bullet under a hedge heading writes its `preferred` row twice
- `Preferred Qualifications:\n- 5 years of experience preferred.` gives two identical
  `experience_years:total_years_preferred` rows: one from the bullet's own view, one from `_views`'
  heading view.
- The verdict is unaffected, because preferred rows never block. It does duplicate persisted
  requirement rows and per-rule counts.
- **Fix:** skip the heading view for a pattern the unit's own view already detected with the same
  captures.
- Found by Codex batch review round 5 (2026-09-22e) and by the T105 executor's open question 3.

### T157 residual (documented, not ticketed) — a boast and a real bar in ONE sentence
- The anchor stands down on any second duration, so both rows stand.
- **For a profile under both bars:** `ineligible`, which is right.
- **For a profile between them** (e.g. 10 years against 25 and 5): the refinement-group straddle
  abstains the pair, giving `uncertain`, a review hold. That is the same as before T157.
- A target-aware suppressor, one that knows which span it is suppressing, needs a new `detect`
  mechanism.

---

## Status at the 2026-09-22f close

- **T135: SHIPPED** (#418 → `04f9ba6d`, D-559).
- **T144: BUILT, reviewed, gated on a stack; NOT merged** (`exec-t144` @ `e5969050`, D-560).
- **T133: BUILT and reviewed; NOT gated or merged** (`exec-t133` @ `29aebdae`, stacked on T144, D-560).
- **T125: rebuilt as an ANNOTATION on the owner's ruling (D-557), BUILT, NOT reviewed** (`exec-t125`
  @ `99640f24`). Run `make web` and commit the bundle and manifest before its gate (D-561).
- **T123, T124: CLOSED on measurement** (D-558).
- **T138: the reverse half is REFUSED** (D-557). The per-kind half is open, with its handoff written
  and its safety measured (0 of 220 rows would fail).
- **T137, T139: open**, with handoffs written. T139 is the stage extraction only; its doc half was
  already done (D-561).
- **T161: design written** (`DESIGN-T161-gate-verdict-identity.md`). The owner's read is owed; the note
  recommends keying BOTH the lane read and the freshness read on the judge's inputs.
- **T162: PARKED** until a switch of judge model or effort is planned (D-557).
- **T163: open. It must follow T161**, because it edits a digested module (D-561).
- The executor handoffs for every open ticket are in `.agent/executor/`. `COMMON.md` now forbids
  backgrounding commands.

---

## Status at the 2026-09-23 close, and three tickets it added

- **SHIPPED, one squash each, in stack order:** T144 (#420), T133 (#422), T137 (#423),
  T138 (#424, the kind half), T139 (#425), T125 (#421, annotate only). D-562.
- **Every astra-review ticket is now closed:** shipped, closed on measurement (T123, T124, D-558),
  or refused (T101, D-544; T138's reverse half, D-557).
- **Still open, all owner-gated or ordered:** T161 (the owner's read of
  `DESIGN-T161-gate-verdict-identity.md` is owed), T163 (after T161), T162 (parked, D-557), T113b
  (only if a stale-`ineligible` backlog recurs), and T164–T166 below.
- **Fixed in the close docs, not ticketed:** the three `RUN_CONTRACT.md` corrections and D-528's
  review-stub claim (D-564).

### T164 — T137's drift report fires once, falsely, on the first run after a taxonomy bump
- `extract/preflight.py:run_preflight`, called inside ranking (`cli/top_cmd.py`), rewrites
  `profile.skills_json` and `taxonomy_version` when the taxonomy version has moved. That write lands
  AFTER `runner._capture_run_start` has read the start identity, and `profile_row_hash` covers both
  columns.
- So that one run reports `run identity drifted mid-run: profile_row_hash`, although the ranker,
  the judge, the ledger stamp and the manifest all read the refreshed profile. The run's own
  preflight made the change; no outside input moved.
- **Reach:** one soft alert, on the first run after each taxonomy version bump. No live run has
  executed T137 yet (it reaches the live run at 473), so the observed population is 0.
- **Fix:** capture the start identity after the preflight's profile refresh, or run that refresh
  before `_capture_run_start`. Do not special-case the field: that would hide a real mid-run
  profile edit. Tests: a taxonomy bump gives `identity_drift == []`; T137's mid-run profile edit
  still names `profile_row_hash`.

### T165 — `verify`'s missing-PDF end-to-end test has never run
- `tests/pipeline/test_verify_cmd.py::test_verify_run_fails_when_a_pdf_is_deleted` skips on every
  run, before and after T138, with `this run produced no PDF to delete (typst unavailable)`.
- The skip reason is wrong: tectonic is installed. The setup writes no résumé template, so nothing
  is tailored and there is no PDF to delete.
- **Fix:** seed the template the way the tailoring pipeline tests do, so the test runs and fails
  when `verify` stops reporting `missing_pdf_file`. Replace the skip with an assertion that a PDF
  was produced, or make the skip name its real cause. T138's shared `file_check` is unit-tested,
  so this is coverage of `verify`'s end-to-end path, not of the rule itself.

### T166 — `doctor` can reap a live run's row: its reap takes no lease
- `cli/doctor_cmd.py` (~120) calls `reap_stale_runs` outside any lock. T133 made the pipeline hold
  `scan_lease` for the whole run, and its reap sits inside that lease; `doctor`'s does not.
- A `doctor` during a run longer than `reap_stale_after_hours` marks that live run's row
  `failed: reaped …` while it is still working. `doctor` is not read-only (memory).
- **Fix (cheap):** try `scan.lock` without blocking. While another process holds it, skip the reap
  and say so in `doctor`'s output ("a run holds the lease; reap skipped"). Otherwise reap as today.
  Test: with an external `FileLock` held and a 3-day-old unfinished row, `doctor` leaves `runs`
  byte-equal; without the lock, it reaps as before.

**Finding, not ticketed (T139's executor):** the gate's refused-verdict branch cannot be reached
through a real `claude` response, because parsing already rejects an unknown decision. It is dead
code or defence in depth. The T139 snapshot reaches it through a stub at `_judge_batch`.

---

## Status at the 2026-09-23b close, and six tickets it added

- **SHIPPED, one squash each:** T165 (#427), T166 (#428), T164 (#429), T161 (#430), T163 (#431),
  T169 (#432), T168 (#433). The 2026-09-23 stack also finished merging (#422–#425, #421; D-568).
- **T161, T163 and T169 reach the live run at 473**, sharing one `engine_version` re-key (D-566 §2):
  473 is T161's live proof (no apply-lane dip across a re-key, `readings_absent` 0).
- **Designed and measured, ruling owed: T170.** **Built and reviewed, merge held for the owner: T172.**
- **Still parked:** T162 (until a judge model or effort switch is planned); T113b (only if a
  stale-`ineligible` backlog recurs; T161 now also keeps a facts/model-changed hide from releasing
  silently, so its reach narrowed to effort changes).

### T167 — DONE (machine only): the job-apps link refresher ran after the day's run
- `~/Library/LaunchAgents/com.boardwatch.jobapps-links.plist` fired at 05:50 and 08:45; runs start at
  04:00. `_eligibility_review` (created 08:43–08:45) was missed on 09-15 and 09-16;
  `_skipped/min_1_year_*` (08:50–08:53) was read a run late every day since 09-17.
- **Owner ruling, executed 2026-09-23:** one slot at 03:45. Backup
  `com.boardwatch.jobapps-links.plist.bak-20260923-pre0345`. Undo: restore it, `launchctl bootout`
  then `bootstrap`.

### T168 — SHIPPED (#433): the job-apps lane dropped an incomplete record uncounted
- Follow-ups, not built: a dangling `discovery_record.json` symlink is never a candidate and stays
  uncounted; a two-root count test.

### T169 — SHIPPED (#432): a second-layout preferred-years row stored a literal `{years}`

### T170 — DESIGNED (`DESIGN-T170-sentence-final-hedge.md`), ruling owed
- A years bar its own sentence calls preferred (`5+ years of experience, not required but
  preferred.`) is read as a hard floor. Recommended mechanism `hedged_by_tail`; prototype `c72feaf1`
  (+ R7 re-pin `57d50f91`) on local branch `exec-t170`, 40 tests.
- **Measured:** 266 of 126,854 pinned postings leave `ineligible` (194 → `uncertain`, 72 →
  `eligible`), none in any other direction; null control 0; 16 of 20 movers read right.
- **Before shipping:** apply the design's described fix for the `with <modifier> … preferred` leak
  (~2% of movers) and re-measure (~45 min). Moves `engine_version` AND `rules_hash`.
- **Timing:** rides run 473's re-key only if merged before the primary pull after run 472.
- **A second owner decision inside it:** hedged SCOPED bars carry no row today, in both the one-line
  and split forms. Carrying them as `preferred` would turn 186 of the new `uncertain` movers
  `eligible`. Separate ticket if wanted.

### T171 — the queue list loads the rules catalog twice, and needs a profile it did not before
- `store/delivery_queries.py:delivered_unapplied` loads `rules.yaml` for the identity and again for
  T161's gate read. Measured on the live store, read-only: 1.8–2.8 s per call for 1,026 rows, ~0.1 s
  per load. With no profile, a malformed rules override now fails the list where the gate read used
  to return nothing (Codex, T161 round 1).
- **Fix:** load once per call and pass it to both reads; skip the gate read's catalog when there are no
  facts. Build only if the queue list's latency matters to the owner.

### T172 — BUILT AND REVIEWED on `exec-t172`, merge HELD for the owner: two applied jobs share a folder name
- Runs 470 and 471 logged `QueueConflictError: SingleStore_Software_Engineer_New_Grad_2027 already
  exists at its destination`. Two distinct APPLIED jobs, `Software Engineer-Helios-New Grad 2027`
  (job 323782, app 206) and `Software Engineer-Engine-New Grad 2027` (247577, app 108), map to one
  folder name; the planner disambiguates only the standing rows, so the drain into `_applied` collides.
- **Fix built:** the reconcile drain widens the destination with the mover's own 8-hex identity
  suffix, via `plan_lead_names`, when the occupant is a DIFFERENT job by reconcile's REFRESHED index.
  A same-job, unknown or doubly-taken occupant still refuses. Codex round 1 blocker (a stale
  `details.json` job id after a D-430 convergence) fixed in `226b62da`; round 2's (the map stale
  within one pass) fixed in `ef3facb4`. Final code head `ef3facb4` (gate EXIT=0, 10,938 passed); rebuilt on `main` as DRAFT PR #434.
- **Owner-visible:** the Helios folder lands in `_applied` as `SingleStore_Software_Engineer_New_Grad_2027
  <suffix>` on the first run after merge. Nothing else is renamed.

## Status at the 2026-09-23c/24 close, and the tickets the sprint added (T173–T192)

- **SHIPPED, one squash each:** T172 (#434), T168b (#437), T171 (#438), T170 (#439), the f10 doc scrub
  (#440), T162 (#441), T179 (#442), T185 (#443), T184 (#444), T192 (#445), T186 (#446), T190 (#447),
  T191 (#448); T187 and T189 ship on their chained gates (see STATE). **The second engine batch**
  (T174/T175, T178, T180, T181, T182 as one re-key on `engine-batch-2b`) is gated, reviewed and
  measured — ship it after reading its two-arm (D-587).
- **Reviewed, gate green, OWNER-GATED:** T188 (D1 re-judges every stored gate verdict).
- **Built, measure on the shipped batch, then rule:** T173.
- **Refused:** T176 (reasoned in the session notes). **Closed as already shipped 2026-09-04:** T177.
- **Machine:** the owner's `role-taxonomy.yaml` (bundled `software`) written 22:18 on 09-23 before T184
  merged; the T186 cutover at 02:17 on 09-24 set `target_countries=[USA]`, added the seven
  platform-developer exclude titles (D-577 §1), and reopened 1,206 decisions.

### T173 — a hedged SCOPED years bar carried as a `scoped_years_preferred` row — BUILT, measure then rule
### T174 / T175 — preferred years twins stop at the clause boundary; an abstaining bar's hedges are read after its abstains — IN THE BATCH
### T176 — REFUSED (the review's premise did not hold on the store; notes in the session dir)
### T177 — the bundle lock — CLOSED, shipped 2026-09-04 (`ae64c0ee`)
### T178 — an experience upper bound is a ceiling, never a minimum — IN THE BATCH
### T179 — SHIPPED (#442): the judge's facts key digests only the facts that are set
### T180 — review F5/F6/F7/F11 on hedge headings, one-line shapes, heading-view spans, plain readings — IN THE BATCH
### T181 — a hedge heading and a tail hedge reach every family's bare-noun bar — IN THE BATCH
### T182 — the employment-terms hold and the graduation seasons no longer assume one tenant — IN THE BATCH (closed choices from the review, D-579)
### T183 — DESIGN-T183 (the multi-tenancy plan): ruled Q1–Q7 (D-578); phases A–E executed as T185–T188
### T184 — SHIPPED (#444): the role gate reads a per-user taxonomy gathered at onboarding (D-581)
### T185 — SHIPPED (#443): every ranker and review gate reports whether it decided on tenant data (D-582)
### T186 — SHIPPED (#446): target countries are profile data; the location gates read them (D-583)
### T187 — the field-dependent gates read the taxonomy's field — REBASED, GATE GREEN, SHIP CHAINED (D-586)
### T188 — the band-relative judge question, Indeed country, GitHub lists — REVIEWED, OWNER-GATED
### T189 — the queue's drains keep what they hold and never discard the owner's files — REBASING (T189e), then gate and ship
### T190 — SHIPPED (#447): a contended queue refresh retried and recorded; the web server's writes at BEGIN; mid-run config drift (D-585)
### T191 — SHIPPED (#448): every lane's self-report recounted from the store
### T192 — SHIPPED (#445): one trickling host cannot pin a run (D-584)

**Follow-ups the reviews left (write as tickets when picked up):** the `_recovered` reserved-name
collision (T189); the funnel's `location_class` column still the US reading (T186); cross-check
disagreements are not soft-alerted (T191, pre-existing for all four); the STANDING-FACTS grounding key
is the taxonomy field (T187); `career_field` vs the taxonomy field unreconciled (D-586); T190's
test-only `model_copy` drift; T192's trickled-headers limit; T188's `any`-band leads judged before a band
is set (closed by T188b's key change once T188 ships).

### T193 — spelled-number years with a parenthesised digit detect nothing ("five (5) years of experience")
Found by the run-474 audit (D-589), both misses hard years bars in delivered leads (221163, 171). `detect()` on
`4943b59c` returns no `experience_years` row for "five (5) years of experience" / "four (4) years of related
experience required"; the digit control ("5 years of experience") detects. An engine change (rules.yaml pattern +
corpus rows + pins); measure on the pinned set before shipping.

### T194 — `Tianjin, Tianjin` classifies as `unknown` in the location catalog
Found by the run-474 audit (D-589): a China posting (250284) passed the location filter. `location_data`'s
foreign city/region map lacks Tianjin (and its municipality twin); add with the ISO-3 mapping T186 introduced,
two-arm over the stored location segments (byte-identical elsewhere).

### T195 — the standing-queue refresh ranks released holds first
Found by the run-474 audit (D-589): after a key change (T179) the refresh re-judges at 130/run in an order that
leaves 14 delivered leads with an off-key `ineligible` standing in the apply lane for ~6 runs. Prefer released
holds (a newest off-key row that would hold) over never-judged leads in the refresh's candidate order. Owner-gated
with the budget question.
