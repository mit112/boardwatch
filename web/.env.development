# The API does not exist yet. Dev serves the app from `src/fixtures/` so the UI is demonstrable
# end to end. Override per load with `?live=1` in the URL; a production build always talks HTTP.
VITE_FIXTURES=1
